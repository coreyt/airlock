You are an implementing agent for Pack 0.5.0-OBS-ledger — retrofit **every request
mutation site** to also record into the one canonical ledger
(`metadata["airlock_mutations"]`) via the shared helpers from `airlock/transparency.py`,
plus add the derived `drop_params` detection. The existing ad-hoc `airlock_*` metadata
keys **stay** (CC-T1, back-compat); you are ADDING the normalized ledger alongside them.
This is observe-only — **no request behavior changes** (CC-T4). This prompt is
**self-contained** — trust it and the on-disk state, not any memory of prior
conversation. You do **not** spawn subagents. You commit inside your worktree; the
**orchestrator** merges worktree → main.

## Environment
Worktree (your working directory): <repo-root>/.claude/worktrees/obs-ledger
Branch: obs-ledger
Base commit (fresh from main): 827d126

Do ALL work inside the worktree. Do NOT cd into <repo-root>.

### CRITICAL FIRST STEP — verify tree, base, venv
```bash
cd <repo-root>/.claude/worktrees/obs-ledger
pwd && git rev-parse --show-toplevel    # must equal the worktree path
git log --oneline -1                     # must show 827d126
git status --short                       # clean
uv run python -c "import airlock; print(airlock.__file__)"   # MUST be under .claude/worktrees/obs-ledger/
uv run python -c "from airlock.transparency import record_mutation, record_redaction, detect_dropped_params; print('ok')"
```
If `git log -1` is older than 827d126, `git merge 827d126 --ff-only` then re-verify.
Venv is pre-synced; if import/pytest missing, `uv sync --all-extras` once. Do NOT touch
`pyproject.toml`/`uv.lock`.

## File Ownership
You MODIFY (source — one record call added per existing mutation, nothing else):
- `airlock/reasoning_effort.py`
- `airlock/fast/router.py`
- `airlock/fast/guardian.py`
- `airlock/guardrails/pii_guard.py`
- `airlock/guardrails/enhanced_interceptor.py`
- `airlock/providers/enhanced_passthrough.py`  ← note path: `providers/`, NOT `guardrails/`
- `airlock/guardrails/reasoning_stripper.py`
- `airlock/gemini_interface.py`
And the matching tests (extend existing files; add ledger assertions):
- `tests/test_reasoning_effort.py`, `tests/test_fast_router.py`, `tests/test_fast_guardian.py`,
  `tests/test_pii_guard.py`, `tests/test_enhanced_interceptor.py`,
  `tests/test_enhanced_passthrough.py`, `tests/test_reasoning_stripper.py`,
  `tests/test_gemini_interface.py`.

You READ ONLY:
- `dev/notes/design-mutation-and-provider-transparency.md` §2 (Decision 1 one-ledger,
  Decision 8 drop_params), §3 (helper signatures), §4 ("Append at the source"),
  CC-T1/CC-T2/CC-T4. **Your spec; it is in your worktree.**
- `airlock/transparency.py` — the helpers you call (signatures below).

You DO NOT TOUCH:
- `airlock/transparency.py` (OBS-core — consume only).
- `airlock/callbacks/*` (OBS-served/headers/log), `airlock/fast/monitor.py`
  (OBS-accounting), `airlock/callbacks/metrics.py` (OBS-metrics), `config.yaml`,
  `pyproject.toml`/`uv.lock`.
If a site genuinely cannot reach `metadata` without changing a function's signature/
contract or touching a DO NOT TOUCH file → **STOP and report** (see the
`enhanced_passthrough` note). Do not absorb a cross-file change silently.

## The helpers (from `airlock/transparency.py` — consume, exact signatures)
```python
record_mutation(metadata: dict, *, field, op, before=None, after=None, stage, source, reason=None) -> None
record_redaction(metadata: dict, *, field, count, category, stage, source) -> None   # value-free (CC-T2)
detect_dropped_params(data: dict, model: str, provider: str) -> list[str]
```
- Both `record_*` append a `Mutation` to `metadata["airlock_mutations"]` (one ledger,
  CC-T1). Get the metadata dict the same way the site already does, or
  `metadata = data.setdefault("metadata", {})` (for the streaming reasoning-stripper,
  `request_data.setdefault("metadata", {})`).
- `MutationOp ∈ {set, drop, clamp, rewrite, inject, redact, suppress}`;
  `MutationStage ∈ {pre_call, during_call, post_call}`.
- **CC-T2:** redactions go ONLY through `record_redaction` (value-free count/category);
  never put scrubbed/secret text in `before`/`after`. For `inject`, do NOT store the
  injected system-prompt text in `after` (keep it `None` or a non-content marker like a
  profile name in `reason`) — the full ledger lands in the JSONL log, so content there
  is a leak risk.

## Per-site map (anchors re-verified at 827d126; re-grep each before editing — they drift)
Record exactly ONE well-formed record per actual mutation (guard so it only fires when
the mutation truly happens, capturing real before/after where the op carries a value):

| # | Site (file:line) | op / field | before→after | stage | source | metadata via |
|---|---|---|---|---|---|---|
| 1 | reasoning_effort.py:57 | set / reasoning_effort | orig→"minimal" | pre_call | reasoning_effort.normalize | data.setdefault("metadata",{}) |
| 2 | reasoning_effort.py:62 | set / reasoning_effort | orig→"disable" | pre_call | reasoning_effort.normalize | " |
| 3 | reasoning_effort.py:66 | drop / reasoning_effort | orig→None | pre_call | reasoning_effort.normalize | " |
| 4 | router.py:520 | rewrite / model | orig→default(e.g. claude-sonnet) | pre_call | router.smart | data.setdefault("metadata",{}) |
| 5 | router.py:606 | rewrite / model | orig→routed | pre_call | router.cost_tier (use the existing reason string) | " |
| 6 | guardian.py:174-175 (record near :199) | suppress / fallbacks | None→None | pre_call | guardian.pin | data.setdefault("metadata",{}) @:177 |
| 7 | guardian.py:281 | rewrite / model | alias→resolved | pre_call | guardian.alias | data.setdefault @:282 |
| 8 | guardian.py:415 | rewrite / model | orig→failover | pre_call | guardian.failover | data.setdefault @:414 |
| 9 | pii_guard.py:200 (count from the pii_map @:203) | **record_redaction** / messages | count, category="pii" | pre_call | pii_guard | data.setdefault("metadata",{}) |
| 10 | pii_guard.py:344 | **record_redaction** / mcp_arguments | count, category="pii" | pre_call | pii_guard.mcp | " |
| 11 | enhanced_interceptor.py:33 | inject / system | None→None (profile in reason) | pre_call | enhanced.interceptor | data.setdefault("metadata",{}) |
| 12 | providers/enhanced_passthrough.py:157 | inject / system | None→None | pre_call | enhanced.passthrough | **see note** |
| 13 | reasoning_stripper.py:179 (non-stream) | rewrite / messages | None→None | post_call | reasoning_stripper | data.get("metadata") or setdefault |
| 14 | reasoning_stripper.py:208 (stream) | rewrite / messages | None→None | post_call | reasoning_stripper.stream | request_data.setdefault("metadata",{}) |
| 15 | gemini_interface.py:71 | set / reasoning_effort | orig→"high" | pre_call | gemini.mode | data.setdefault @:81 |
| 16 | gemini_interface.py:73 | set / reasoning_effort | orig→"disable" | pre_call | gemini.mode | " |

**Site 12 — `enhanced_passthrough.py:157` (provider-side, NO metadata in scope).** The
injection writes into a local `resolved_messages`, and `metadata`/`data` may not be
reachable there without a signature change. First investigate (grep up the function for
`data`/`metadata`/a request object). If metadata IS reachable cleanly → record as row
12. If it is genuinely NOT reachable without changing the function's contract or a DO
NOT TOUCH file → **STOP and report it as a blocker** (do not thread a new parameter
through on your own — that is the orchestrator's call). Record what you found either way.

**Site 17 — derived `drop_params` (Decision 8).** In the guardian PRE-CALL path, after
the final target model+provider are resolved, call
`dropped = detect_dropped_params(data, model, provider)` and record each as
`record_mutation(metadata, field=<param>, op="drop", before=None, after=None,
stage="pre_call", source="drop_params", reason="provider-unsupported (drop_params)")`.
Provider resolution sites: `guardian.py:291` (pinned path, `provider = infer_provider(model_name)`)
and `guardian.py:433` (general path, `target_provider = infer_provider(...)` after all
rewrites). Call detection **once per request** at the resolved provider — pick the point
where `data`, the final `model`, and the resolved provider are all in scope; avoid
double-recording across the two branches. Do NOT put param VALUES in the record (some
may be sensitive) — field=param name only.

## Target Tests
```bash
cd <repo-root>/.claude/worktrees/obs-ledger
uv run python -m pytest tests/test_reasoning_effort.py tests/test_fast_router.py tests/test_fast_guardian.py tests/test_pii_guard.py tests/test_enhanced_interceptor.py tests/test_enhanced_passthrough.py tests/test_reasoning_stripper.py tests/test_gemini_interface.py -q
```
Assert (TDD, RED first, no-network):
- Each site, when its mutation fires, appends **exactly one** well-formed record to
  `metadata["airlock_mutations"]` with the right field/op/stage/source (and before/after
  where the op carries a value). When the mutation does NOT fire, no record is added.
- PII sites use `record_redaction` and the record is **value-free** — assert the
  scrubbed secret string is absent from the entire `airlock_mutations` structure.
- `drop_params`: a client param unsupported by the resolved provider produces an
  `op="drop"` record; a supported param does not; no metadata/airlock-internal key is
  flagged.
- **CC-T1 back-compat**: the existing `airlock_*` keys (`airlock_routing`,
  `airlock_alias`, `airlock_failover`, `airlock_pii_map`, `airlock_pinned_request`,
  `airlock_gemini`, …) are still written exactly as before (assert at least the ones
  your touched sites set).
- **CC-T4 no behavior change**: the request outcome (final model, suppression flags,
  scrubbed payload) is unchanged — only the added ledger is new.

## Development Cycle: PLAN → RED → READ → GREEN → LINT → COMMIT → CLOSE → REPORT
0. **PLAN** memo → `dev/notes/0.5.0-OBS-ledger-design.md` (worktree): the per-site
   record plan + the enhanced_passthrough investigation result + the drop_params call
   point. Commit; echo `[0.5.0-OBS-ledger][PLAN]`.
1. **RED** — add the ledger-assertion tests across the eight test files; run; confirm
   FAIL; commit tests-only; record red sha. Work site-by-site if that helps you keep
   each test honest.
2. **READ** — only the READ ONLY targets.
3. **GREEN** — add the record calls site by site; run the affected test file after each.
   Follow existing code style; do NOT refactor surrounding code or change any existing
   behavior. If a site fights you past 3 attempts, record it and move on — a partial
   ledger that's correct for the sites it covers is mergeable; flag the rest.
4. **LINT** — `uv run ruff check` + `ruff format --check` on every file you changed; fix.
5. **COMMIT** — scoped `git add` of exactly the files you changed (NOT `-A`):
   ```bash
   git commit -m "feat(transparency): OBS-ledger — record every mutation site into the ledger + drop_params detection"
   git log --oneline -1
   ```
   Never `--no-verify`; never push/merge/touch main.
6. **CLOSE** — write `dev/plans/runs/0.5.0-OBS-ledger-output.json` LAST (schema in
   `.claude/agents/implementer.md` §6), even on a blocker. List any site you could NOT
   complete (esp. enhanced_passthrough) in `blockers_encountered`.
7. **REPORT** — the §7 structure, incl. `output_json` path and which sites are covered
   vs flagged.

## Console logging contract
`[0.5.0-OBS-ledger][PHASE]` at PLAN/RED/GREEN/COMMIT; `[DETECT]`/`[RESOLVE]` on any
problem (esp. site 12); `[SUMMARY]` listing covered/flagged sites. Mirror into
`output.json.blockers_encountered`.

## Scope discipline
- **CC-T1**: never remove or rename an existing `airlock_*` key — only ADD ledger records.
- **CC-T4**: never change a mutation's actual effect, ordering, or condition. If adding a
  record would require restructuring the mutation logic, the restructuring is out of
  scope — record at the existing mutation point or flag it.
- No edits to DO NOT TOUCH files. Escalate, don't absorb, any cross-file dependency.

## Definition of Done (the reviewer's bar)
Every reachable mutation site appends exactly one well-formed ledger record (right
field/op/stage/source); PII sites are value-free via `record_redaction`; `drop_params`
detection records `op:drop` once per request at the resolved provider; the existing
`airlock_*` keys and all prior behavior are unchanged (CC-T1/CC-T4); any unreachable
site (enhanced_passthrough) is clearly flagged in output.json rather than forced; target
tests green, lint clean, committed, output.json written.
