You are an implementing agent for Pack **0.5.4-MIGRATE-entfathom-cutover** (2b-ii) —
**activate** the recorder as the single live telemetry callback in enterprise's slot,
make enterprise+fathom pure sinks, and **delete** the old duplicated builders. This is
the behavior-preserving cutover. Self-contained prompt; you commit in your worktree;
the orchestrator merges.

**Contract: the live logged/served surface is UNCHANGED** (the one accepted internal
delta is `timestamp` convergence, already registered). Every enterprise/fathom record
must be field-for-field identical to today. Getting the ordering / no-double-emit /
fathom-gating right is the whole job.

## Environment
Worktree only; never cd into the main checkout. Reconcile FIRST:
```bash
git rev-parse --show-toplevel
git merge feat/0.5.4-eventbus --ff-only        # brings EVENT + 2a projections + 2b-i wire
git log --oneline -3                            # expect "Merge Pack 0.5.4-MIGRATE-entfathom-wire (2b-i)" near HEAD
test -f airlock/callbacks/recorder.py && grep -q "record_event" airlock/callbacks/enterprise_logger.py && echo OK
```
If not a clean ff or toplevel is the main checkout, STOP and report.
Tests: `uv run --extra test python -m pytest …`.

## File Ownership
You **MODIFY**:
- `config.yaml` — `success_callback`/`failure_callback` (replace enterprise proxy_logger with the recorder, BEFORE proxy_monitor)
- `airlock/callbacks/recorder.py` — add `_self_register()` (live install) + flag-gate the fathom sink
- `airlock/callbacks/enterprise_logger.py` — DELETE the old builder + callback methods + `_self_register`; keep the sink + helpers
- `airlock/callbacks/fathom_logger.py` — DELETE the old builder/projection/callback methods + `_self_register_async`; keep the sink + helpers
- `airlock/proxy.py` — remove the now-redundant fathom-callback append (the recorder owns fathom)
- `tests/test_projections_equiv.py` — convert the equivalence ORACLE to frozen snapshots (see step 1)
- `tests/test_enterprise_logger.py`, `tests/test_fathom_logger.py` — adapt tests of deleted symbols
- `tests/test_recorder_cutover.py` (NEW) — the seam tests
You **READ ONLY**: `airlock/callbacks/request_event.py`, `airlock/callbacks/projections.py`,
`airlock/fast/monitor.py` (`log_failure_event` :249-364 sets `airlock_provider_protection`),
`dev/notes/design-request-event-bus.md` §5/§5a/§5b.
You **DO NOT TOUCH**: `s3_logger.py`, `sql_logger.py`, `metrics.py`, `monitor.py`
(s3/sql/sidechannels are later packs; monitor stays as-is).

## Resolved facts (verified at HEAD — do not re-derive)
- **Ordering mechanism:** today config-entry-order → import-order → `_self_register()`
  append-order guarantees enterprise BEFORE monitor in all 4 callback lists. monitor
  sets `metadata["airlock_provider_protection"]` in its **`log_failure_event`**
  (monitor.py:326-339) — so on the FAILURE path the recorder MUST build the event
  (snapshot `guardrail_meta`) before monitor runs, or enterprise's record would gain
  `airlock_provider_protection` (a regression). Replicate by: recorder_callback FIRST
  in config.yaml + `recorder._self_register()` appending to all 4 lists at import.
- **Fathom gating:** fathom is active only when `AIRLOCK_ENABLE_FATHOM_LOGGER` is set
  (proxy.py:155-156, 200-209 appends the fathom callback today). The recorder must
  preserve this: register the fathom sink only when the flag is set.
- The 2b-i `record_event` sink methods + `RequestRecorderCallback` already exist and
  are codex-verified faithful. The recorder builds the event ONCE and dispatches.

## Steps (do them in this order)

### 1. FREEZE the equivalence oracle (BEFORE any deletion)
`tests/test_projections_equiv.py` currently compares `project_*` against the LIVE
`_build_record`/`_fathom_properties`. Those are about to be deleted, which would
destroy the oracle. Convert it: capture the current builders' output for the
representative request set as **frozen golden fixtures** (e.g. write the expected
dicts to a `tests/fixtures/0.5.4-entfathom-golden.json` or inline frozen literals),
and rewire the tests to assert `project_enterprise(event)`/`project_fathom(event)`
equal the **frozen** expected (still ignoring `timestamp`). Run once against the live
builders to confirm the snapshots are exact, THEN freeze. After this step the
projection equivalence proof no longer depends on the live builders.

### 2. ACTIVATE the recorder (live install, ordered, fathom-gated)
- `recorder.py`: add `_self_register()` mirroring enterprise's (add recorder_callback
  to `add_litellm_success_callback`/`failure`/`async_success`/`async_failure`), called
  at module level. Gate the fathom sink registration:
  `if _env_flag("AIRLOCK_ENABLE_FATHOM_LOGGER", default=False): request_recorder.register(proxy_fathom_logger.record_event, name="fathom", async_only=True)`
  (enterprise sink registers unconditionally, FIRST/always).
- `config.yaml` :548-549: `success_callback` and `failure_callback` →
  `["airlock.callbacks.recorder.recorder_callback", "airlock.fast.monitor.proxy_monitor"]`
  (recorder BEFORE monitor; preserves the ordering invariant).
- `proxy.py`: remove the `_fathom_logger_enabled()` block that appends
  `airlock.callbacks.fathom_logger.proxy_fathom_logger` to the callbacks list
  (~lines 200-209) — the recorder now owns fathom dispatch (gated by the same flag).
  Keep any startup log message if trivial, else remove the whole block.

### 3. DELETE the old duplicated paths (the deliverable)
- `enterprise_logger.py`: delete `AirlockLogger._build_record`, `log_success_event`,
  `log_failure_event`, `async_log_success_event`, `async_log_failure_event`, and the
  module-level `_self_register()` definition + its call. **KEEP**: `proxy_logger`
  instance, `record_event`, `_write_log`, `_redact_record`, `write_precall_block_record`,
  `write_batch_record`, `write_admin_action_record`, `_serialize`, `_normalize_failure`,
  `_get_airlock_client`, log rotation/cleanup, and `_patch_lowest_cost_none_guard`
  (keep the lowest-cost patch + its call — unrelated to telemetry).
- `fathom_logger.py`: delete `_log_event`, `_base_record`, `_fathom_properties`,
  `log_success_event`, `log_failure_event`, `async_log_success_event`,
  `async_log_failure_event`, and `_self_register_async` def + its call. **KEEP**:
  `proxy_fathom_logger`, `record_event`, `_should_skip_call_id`, `_get_engine`,
  `_serialize`/`_json_text`/`_response_text`/`_env_flag`, the class + dedup state.
- Re-grep both modules after deletion for any now-dangling internal reference.

### 4. ADAPT the broken tests (NO faked greens)
Deleting the builders breaks ~20 `AirlockLogger._build_record(...)` tests in
`test_enterprise_logger.py` and the `log_success_event`/`log_failure_event` tests in
both files. For EACH broken test:
- If it asserted **field values** of the old builder → that coverage is now in the
  frozen golden oracle (step 1) + `test_request_event.py`; **remove** the obsolete
  test and record the mapping (old test → covering new test) in
  `output.json.additional_changes_made_in_scope`.
- If it asserted **behavior** still present (e.g. `_write_log` disk-full safety,
  precall-block/batch/admin records, rotation, redaction) → KEEP it; if it reached
  that behavior via a deleted method, re-point it through `record_event` or the kept
  function. 
- **NEVER weaken/delete an assertion to force green** for behavior that still exists.
  If unsure whether coverage is preserved, KEEP and re-point rather than delete.

### 5. SEAM tests (`tests/test_recorder_cutover.py`)
1. **Snapshot-immutability / ordering:** build the event from metadata WITHOUT
   `airlock_provider_protection`; then mutate the metadata dict to add it; assert the
   already-built event's `guardrail_meta` (and `project_enterprise(event)`) still
   EXCLUDE it (proves enterprise's record is pre-monitor as long as the recorder runs
   first). Also assert config.yaml lists the recorder before `proxy_monitor`.
2. **No double-emit:** with the recorder active, one `recorder_callback` async-success
   invocation → enterprise `_write_log` called exactly once and (flag on) fathom write
   once; assert the old `proxy_logger`/`proxy_fathom_logger` are NOT separately in the
   litellm async lists (only recorder_callback + monitor).
3. **Fathom gating:** with `AIRLOCK_ENABLE_FATHOM_LOGGER` unset, the recorder has no
   fathom sink (no fathom write on dispatch); set → fathom sink present. fathom stays
   **async_only** (no fathom on a sync `is_async=False` dispatch). Skip flag honored.
4. **Failure isolation:** a raising sink doesn't break the request or the other sink.

### 6. Cycle & DoD
PLAN→RED→READ→GREEN→LINT→COMMIT→CLOSE→REPORT. Design memo
`dev/notes/0.5.4-MIGRATE-entfathom-cutover-design.md`. Commit scoped files; never
`--no-verify`. Write `dev/plans/runs/0.5.4-MIGRATE-entfathom-cutover-output.json` LAST.
**The FULL no-network suite must be green** (run it, `| tail`): the frozen goldens,
`test_request_event.py`, `test_recorder_wiring.py`, the adapted enterprise/fathom
tests, and the new seam tests. 3-iteration cap on GREEN — if the test adaptation
balloons past that, STOP and report (the orchestrator may split the pack).

**Definition of Done:** `_build_record` (enterprise), `_base_record`/`_fathom_properties`
(fathom) and the old callback methods + self-registers are GONE; the recorder is the
single live callback in enterprise's slot (before monitor), fathom-gated + async-only;
config.yaml + proxy.py updated; no sink emits a different record than today (frozen
goldens + seam tests prove it); no double-emit; full no-network suite green; output.json
last with the deleted-test→replacement-coverage mapping.
