You are an implementing agent for Pack **0.5.1-SET-warnratio** — collapse the two
divergent "near budget" warn ratios (monitor `0.8` / router `0.9`) into one configurable
`budget_warn_ratio` sourced from `AirlockSettings`. This prompt is **self-contained**.
You do **not** spawn subagents. You commit inside your worktree; the **orchestrator**
merges.

## Environment

Worktree (your working directory): `<repo-root>/.claude/worktrees/0.5.1-SET-warnratio`
Branch: `feat/0.5.1-SET-warnratio`
Base commit (fresh from `feat/0.5.1-settings`, AFTER SET-loader AND SET-unify merged): `f2706e9`

Do ALL work inside the worktree. Do NOT cd into `<repo-root>`. Do NOT
edit/stage/commit in the main checkout.

Verify first:
```bash
cd <repo-root>/.claude/worktrees/0.5.1-SET-warnratio
git rev-parse --show-toplevel     # must equal the worktree path
git log --oneline -1              # must show f2706e9 (or a descendant)
git status --short                # must be clean
test -f airlock/fast/settings.py && echo "settings.py present (SET-loader merged) OK"
# SET-unify is merged here: get_settings() + router.known_model_aliases() exist.
grep -q "def known_model_aliases" airlock/fast/router.py && echo "known_model_aliases present OK"
```
If `git log` shows a commit OLDER than `f2706e9`, run
`git merge f2706e9 --ff-only` then re-verify. If any check fails, STOP/report.

## The duplication this pack removes (R3)

One concept, two values, two override stories:
- monitor `_DEFAULT_BUDGET_WARN_RATIO = 0.8` (env `AIRLOCK_BUDGET_WARN_RATIO`),
  `monitor.py:39` + `_budget_warn_ratio()` (~61–68).
- router `_BUDGET_WARN_THRESHOLD = 0.9` (hardcoded, NO override), `router.py:~76`, used in
  `_apply_budget_awareness` (~445–492) to decide the proactive swap point.

`AirlockSettings.budget_warn_ratio` already exists (SET-loader), default `0.8`, sourced
`env AIRLOCK_BUDGET_WARN_RATIO > airlock_settings.budget_warn_ratio > default 0.8`.

## Design decisions (already resolved)

- **Single ratio.** Both the monitor warn and the router proactive swap read
  `get_settings().budget_warn_ratio`. Delete `router._BUDGET_WARN_THRESHOLD` and (if not
  already removed by SET-unify) consolidate the monitor's `_budget_warn_ratio()` onto the
  same source. Re-grep both anchors at your base commit — SET-unify may have already moved
  the monitor's warn-ratio read onto `get_settings()`; if so, only the router side remains.
- **Behavior change #2 (intended, document it):** the router swap point moves from `0.9`
  to the configured ratio (default `0.8`). This is a release-notes item; add/confirm the
  changelog/behavior-register entry. Do NOT silently preserve `0.9`.
- **`0 ⇒ no enforcement` unaffected.** The warn ratio only scales an existing positive
  budget; a `0`/absent budget still short-circuits to no-warn/no-swap upstream. Do not
  change that contract.
- **Client-facing surface (harness coverage REQUIRED).** Moving the swap point changes
  when `X-Airlock-Model-Override` appears and when `X-Airlock-Budget-State: near_limit`
  fires. Per the orchestrator's testing rule, a client-facing change MUST have harness
  coverage: extend `dev/smoketest/` with a budget/warn-ratio scenario asserting
  `X-Airlock-Budget-State: near_limit` fires at the configured ratio (do NOT run it
  against any live service — the orchestrator runs it on a separate dir+port at sign-off).

## What to change

1. **`airlock/fast/router.py`** — delete `_BUDGET_WARN_THRESHOLD`; in
   `_apply_budget_awareness`, use `get_settings().budget_warn_ratio` for the swap-point
   comparison. Re-grep the anchor first.
2. **`airlock/fast/monitor.py`** — ensure the warn path uses
   `get_settings().budget_warn_ratio` (remove the standalone `_budget_warn_ratio()` /
   `_DEFAULT_BUDGET_WARN_RATIO` if still present). If SET-unify already did this, no-op
   here and note it.
3. **`dev/smoketest/`** — add the warn-ratio scenario (added, not run).
4. **Changelog / behavior register** — confirm behavior-change #2 is recorded (the plan's
   behavior-change register lists it; ensure a user-facing changelog entry exists if the
   repo keeps one — check for a CHANGELOG; if none, note it in output.json).

## ALSO IN THIS PACK — test-isolation fix (commit it SEPARATELY from the warn-ratio work)

A release-introduced fragility must be fixed here (commit it as its own clearly-labelled
commit so the reviewer sees it distinctly): SET-unify added failover filtering via
`router.known_model_aliases()`, which reads the **module-global** `router._alias_provider_map`
(populated by `set_router_config`). The guardian failover tests
(`tests/test_fast_guardian.py::TestGuardianPreCallHook::test_unpinned_request_fails_over_and_sets_override_metadata`
and `::TestGuardianLedger::test_failover_records_model_rewrite`) assume that catalog is EMPTY
but do not enforce it, so they FAIL when a catalog-populating test (e.g. `test_fast_router.py`)
ran earlier in the same session. They pass in default alphabetical collection order but the
order-dependence is real.

Fix: add an **autouse fixture in `tests/conftest.py`** that resets the router catalog to a
known-empty state before each test (`from airlock.fast.router import set_router_config;
set_router_config(None)` — confirm `set_router_config(None)` clears `_alias_provider_map`; if
it doesn't accept `None`, reset the global cleanly another way), so failover-filter tests are
order-independent. Verify by running a catalog-populating file BEFORE the guardian file in one
invocation, e.g.:
`uv run --extra test python -m pytest tests/test_fast_router.py tests/test_fast_guardian.py -q`
— both guardian failover tests must pass. (Before your fix, that exact command FAILS those two
tests — reproduce the RED first.)

## File Ownership

You MODIFY: `airlock/fast/router.py`, `airlock/fast/monitor.py`, `tests/conftest.py`
(the autouse isolation fixture), `dev/smoketest/`, `tests/`, and a CHANGELOG if one exists. You DO NOT TOUCH: `airlock/fast/settings.py`
(the ratio field already exists), `airlock/fast/state.py`, `config.yaml` budget VALUES,
`pyproject.toml`, `uv.lock`. If a fix needs a file outside MODIFY, STOP and report.

## Development Cycle: PLAN → RED → READ → GREEN → LINT → COMMIT → CLOSE → REPORT

- **PLAN:** brief memo `dev/notes/0.5.1-SET-warnratio-design.md` (worktree). Commit it.
- **RED (commit tests alone; record red sha):**
  1. **Warn-ratio unification:** with a positive budget and a configured ratio, the router
     swap point fires at `spend >= budget * ratio` for the SAME ratio the monitor warns at
     (assert the router no longer uses `0.9`). Set `budget_warn_ratio` (via config or env)
     and assert both monitor warn and router swap use it.
  2. **Default moved:** unconfigured ratio defaults to `0.8` (so the router swap point is
     now `0.8`, not `0.9`).
  3. **`0` budget still no-op:** a `0`/absent budget yields no warn and no swap regardless
     of ratio.
- **READ:** the router/monitor warn anchors (re-grep at base).
- **GREEN:** wire both onto `get_settings().budget_warn_ratio`; delete the dead constants.
  Max 3 attempts; STOP/report if stuck.
- **LINT:** `uv run ruff check <modified>` + `format --check`; clean.
- **VERIFY:** `uv run --extra test python -m pytest` on the target + a fast/ regression
  sweep (`tests/test_fast_router.py tests/test_fast_monitor.py tests/test_fast_settings.py`).
  (Plain `uv run pytest` may resolve the wrong Python — use `--extra test python -m pytest`.)
- **COMMIT:** scoped `git add`; never `--no-verify`; never push.
- **CLOSE:** write `dev/plans/runs/0.5.1-SET-warnratio-output.json` LAST.
- **REPORT:** output.json path, branch, final HEAD sha, worktree path, red/green shas,
  whether the monitor side was already done by SET-unify, the smoketest scenario added,
  and any `additional_changes_made_in_scope`.

## Console logging contract

`[SET-warnratio][PHASE]` at PLAN/RED/GREEN/COMMIT; `[DETECT]`/`[RESOLVE]` on problems;
final `[SUMMARY]`. Mirror DETECT/RESOLVE into `output.json.blockers_encountered`.

## Definition of Done (the bar the reviewer checks)

1. One configurable `budget_warn_ratio` drives BOTH the monitor warn and the router swap;
   `_BUDGET_WARN_THRESHOLD` (and any duplicate monitor ratio constant) is gone.
2. The router swap point now equals the configured ratio (default `0.8`), proven by test;
   behavior-change #2 recorded.
3. `0`/absent budget still no-warn/no-swap; AC-0 unbroken.
4. `dev/smoketest/` warn-ratio scenario added (not run); fast/ suite green; ruff clean.
5. **Test-isolation fix committed (separate commit):** the autouse `tests/conftest.py` catalog
   reset makes the guardian failover tests order-independent — proven by running
   `tests/test_fast_router.py tests/test_fast_guardian.py` together (both guardian failover
   tests green), where that command failed before the fix.
6. No edits outside MODIFY; nothing pushed; output.json last.
