You are an implementing agent for Pack **0.5.1-SET-unify** — migrate the
budget/failover/warn consumers onto the typed `AirlockSettings` (`get_settings()`),
delete the hidden hardcoded defaults, fix the R6 nesting bug, and ship the config
budget-doc note. This prompt is **self-contained** — trust it and the on-disk state.
You do **not** spawn subagents. You commit inside your worktree; the **orchestrator**
merges.

## Environment

Worktree (your working directory): `/home/coreyt/projects/airlock/.claude/worktrees/0.5.1-SET-unify`
Branch: `feat/0.5.1-SET-unify`
Base commit (fresh from `feat/0.5.1-settings`, AFTER SET-loader merged): `6af83f7`

Do ALL work inside the worktree. Do NOT cd into `/home/coreyt/projects/airlock`. Do NOT
edit/stage/commit files in the main checkout.

Verify first:
```bash
cd /home/coreyt/projects/airlock/.claude/worktrees/0.5.1-SET-unify
git rev-parse --show-toplevel     # must equal the worktree path
git log --oneline -1              # must show 6af83f7 (or a descendant)
git status --short                # must be clean
# Confirm SET-loader is present:
test -f airlock/fast/settings.py && echo "settings.py present OK"
```
If `git log` shows a commit OLDER than `6af83f7`, run
`git merge 6af83f7 --ff-only` then re-verify. If any check fails, STOP/report.

## Context — SET-loader (already merged) gives you the seam

`airlock/fast/settings.py` exposes a frozen `AirlockSettings` and `get_settings()`:
- `provider_budgets: dict[str, float]` — read from `router_settings.provider_budget_config`
  via LiteLLM `BudgetConfig` (correct nesting; `0` preserved as `0.0`).
- `budget_windows: dict[str, float]` — per-provider window seconds (from `time_period`).
- `failover_map: dict[str, list[str]]` — from `router_settings.fallbacks` (list-of-1-key
  dicts already converted to a dict).
- `cost_tiers`, `session_ttl`, `smart_thresholds`, `budget_warn_ratio` (defaults 0.8).

The conversions you'd otherwise write (fallbacks→dict; correct budget nesting; BudgetConfig
parsing) are ALREADY done in `settings.py`. Your job is to **wire consumers onto it and
delete the now-duplicated source-of-truth copies.**

## Design decisions (already resolved — HITL-confirmed; do not re-litigate)

- **Q1 (behavior change, intended):** budgets are config-driven. The hidden
  `50/50/25/25` provider-budget defaults are REMOVED. With no `provider_budget_config`,
  there is **no proactive auto-swap** and **no monitor warn**. Document this; do not
  preserve hidden defaults. (This is the gated behavior change — the orchestrator has
  HITL-confirmed it before this pack merges.)
- **R2 fix:** the stale `_DEFAULT_FAILOVER_MAP` (`gpt-4o`/`gpt-4o-mini` targets that are
  not in `model_list`) is REMOVED. Failover derives purely from `router_settings.fallbacks`.
- **R6 fix:** the monitor must read budgets from the `router_settings` nesting — achieved
  by reading `get_settings().provider_budgets` (which already reads the right place),
  NOT the old top-level `config.get("provider_budget_config")`.
- **`0 ⇒ no enforcement` contract (AC-0):** preserve and document that a falsy/`0` budget
  means *no block (LiteLLM) / no warn (monitor) / no swap (router)* identically across all
  three layers. `get_settings().provider_budgets` returns `0.0` for a `0`-configured
  provider and the key is absent for an unconfigured one — both must yield "no warn / no
  swap".
- **Warn ratio stays split for now.** Do NOT collapse the monitor 0.8 / router 0.9 warn
  ratios — that is the next pack (SET-warnratio). You only change the budget/failover
  *source*. (If migrating the monitor naturally routes its warn-ratio through
  `get_settings().budget_warn_ratio`, that's acceptable since the default is the monitor's
  existing 0.8 — but do not touch `router._BUDGET_WARN_THRESHOLD=0.9`.)

## What to change

1. **`airlock/fast/settings.py`** — change the two budget/failover defaults to remove the
   hidden values: `_DEFAULT_PROVIDER_BUDGETS = {}` and `_DEFAULT_FAILOVER_MAP = {}` (and
   update the `AirlockSettings` field default_factories + the module comment accordingly).
   KEEP the non-budget defaults (`_DEFAULT_COST_TIERS`, `_DEFAULT_SESSION_TTL`,
   `_DEFAULT_SMART_THRESHOLDS`, `_DEFAULT_BUDGET_WARN_RATIO`) unchanged.
2. **`airlock/fast/router.py`** — replace `_DEFAULT_PROVIDER_BUDGETS` (lines ~68–74) and
   `_load_provider_budgets` (~366–374) usage in `_apply_budget_awareness` with
   `get_settings().provider_budgets`. DELETE the now-dead `_DEFAULT_PROVIDER_BUDGETS`
   constant and `_load_provider_budgets` function. Do NOT touch `_BUDGET_WARN_THRESHOLD`
   (SET-warnratio owns it). Re-grep these anchors before editing — they may have drifted.
3. **`airlock/fast/monitor.py`** — fix R6: replace `configure_budgets` (the top-level
   `config.get("provider_budget_config")` read, lines ~48–58) and `_explicit_budget_for`
   (~71–85) so the monitor's budget source is `get_settings().provider_budgets`
   (env override is already handled inside `get_settings()` via `AIRLOCK_PROVIDER_BUDGETS`,
   so the duplicate env read in `_explicit_budget_for` is removed). Preserve the
   `if not limit: return False` (`0`/absent ⇒ no warn) semantics in `_maybe_warn_budget`
   (~88–116). `configure_budgets` may become a thin shim or be removed if `proxy.py`
   no longer needs it — if you remove it, also drop its call in `proxy.py` startup wiring
   and say so (escalate-by-noting in `additional_changes_made_in_scope`, since `proxy.py`
   is shared).
4. **`airlock/fast/circuit_breaker.py`** — replace `_DEFAULT_FAILOVER_MAP` (~32–38) and
   `_load_failover_map` (~46–59) with `get_settings().failover_map`. DELETE the stale
   constant + the env-only loader. (Env override `AIRLOCK_FAILOVER_MAP` is already handled
   inside `get_settings()`.)
5. **`config.yaml` + `airlock/cli/templates/config.yaml`** — add an explanatory comment
   block above `provider_budget_config` (config.yaml ~line 355; template ~188) documenting
   that **one number drives three behaviors**: (1) LiteLLM's hard block (request refused at
   limit), (2) the monitor's near-limit warn (`X-Airlock-Budget-State: near_limit`), and
   (3) the fast router's proactive model reroute as spend approaches the limit — and that
   **`0` means no enforcement / unlimited / no reroute** (the falsy short-circuit). Note
   the `1d` window and the `AIRLOCK_PROVIDER_BUDGETS` env override. Do NOT change any budget
   VALUES (they are `0` already) and do NOT relocate the key.

## File Ownership

You MODIFY: `airlock/fast/settings.py`, `airlock/fast/router.py`,
`airlock/fast/monitor.py`, `airlock/fast/circuit_breaker.py`, `config.yaml`,
`airlock/cli/templates/config.yaml`, `proxy.py` (ONLY if you remove `configure_budgets` —
adjust its call site), and `tests/` (new/updated tests).

You DO NOT TOUCH: `airlock/fast/state.py` (STORE-seam owns it), `router._BUDGET_WARN_THRESHOLD`
and the warn-ratio collapse (SET-warnratio), `pyproject.toml`, `uv.lock`. If a fix needs a
file outside MODIFY, STOP and report.

## Development Cycle: PLAN → RED → READ → GREEN → LINT → COMMIT → CLOSE → REPORT

- **PLAN:** short design memo at `dev/notes/0.5.1-SET-unify-design.md` (worktree): the
  migration map (which consumer reads which `get_settings()` field), what gets deleted,
  the R6 fix, the AC-0 contract, and the test plan. Commit it.
- **RED (commit tests alone first; record red sha):**
  1. **R6 regression:** a budget set under `router_settings.provider_budget_config` is
     captured by the monitor's budget path (today it reads top-level → always empty).
  2. **R2 / failover-from-fallbacks:** a circuit-open failover lands on a model that
     EXISTS in `model_list` (derived from `fallbacks`), never the stale `gpt-4o`.
  3. **AC-0:** one test asserts identical "`0` ⇒ no block / no warn / no swap" across the
     monitor warn and the router swap (and document the LiteLLM-filter side).
  4. **Behavior change #1:** with NO `provider_budget_config`, there is no proactive swap
     and no monitor warn (the hidden defaults are gone).
  5. **Scoped golden (FIX-5):** empty `airlock_settings:` reproduces today's defaults for
     the NON-budget settings (session_ttl, smart_thresholds, cost_tiers, warn-ratio) — and
     explicitly does NOT assert preserved provider-budget defaults.
- **READ:** the files/ranges you're editing (re-grep all anchors at your base commit).
- **GREEN:** wire consumers onto `get_settings()`, delete the duplicated defaults/loaders,
  add the config docs. Follow existing patterns; no unrelated refactors. Max 3 attempts
  per failing test; STOP/report if stuck.
- **LINT:** `uv run ruff check <modified>` + `uv run ruff format --check <modified>`; clean.
- **VERIFY:** run the target tests AND a regression sweep over `tests/test_fast_router.py
  tests/test_fast_monitor.py tests/test_circuit_breaker*.py tests/test_fast_settings.py`.
  NOTE the environment quirk: use **`uv run --extra test python -m pytest ...`** (plain
  `uv run pytest` may resolve a wrong-Python user-site pytest where litellm is missing).
- **COMMIT:** stage only scoped files (no `git add -A`); never `--no-verify`; never push.
- **CLOSE:** write `dev/plans/runs/0.5.1-SET-unify-output.json` LAST.
- **REPORT:** output.json path, branch, final HEAD sha, worktree path, red/green shas,
  whether you removed `configure_budgets` (and the proxy.py adjustment), and any
  `additional_changes_made_in_scope`.

## Console logging contract

`[SET-unify][PHASE]` at PLAN/RED/GREEN/COMMIT; `[DETECT]`/`[RESOLVE]` on problems; final
`[SUMMARY]`. Mirror DETECT/RESOLVE into `output.json.blockers_encountered`.

## Definition of Done (the bar the reviewer checks)

1. `_DEFAULT_PROVIDER_BUDGETS` and `_DEFAULT_FAILOVER_MAP` no longer exist as hidden
   value-carrying defaults anywhere (router, circuit_breaker, settings all empty/derived);
   budgets + failover derive from config via `get_settings()`.
2. R6 fixed (monitor reads the `router_settings` nesting via `get_settings()`); R6
   regression test green.
3. R2 fixed (failover lands on a real `model_name`); test green.
4. AC-0 preserved + documented (`0` ⇒ no block/warn/swap, all three layers); test green.
5. Behavior-change #1 covered by tests (no config budget ⇒ no swap/warn).
6. Scoped golden (FIX-5) green; full fast/ regression suite green; ruff clean.
7. config.yaml + template budget-doc note present; NO budget VALUE changes; NO key moves.
8. Warn-ratio collapse NOT done here; state.py untouched; nothing pushed; output.json last.
