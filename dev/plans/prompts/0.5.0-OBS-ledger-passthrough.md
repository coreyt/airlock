You are an implementing agent for follow-up Pack 0.5.0-OBS-ledger-passthrough — close the
one mutation-ledger gap codex found in OBS-ledger: the **enhanced-passthrough
config-fallback system-prompt injection is not recorded** in the ledger. Site 11
(`enhanced_interceptor`) only records when `litellm_params["enhanced_profile"]` is present;
a model whose enhanced profile is resolved from the **config cache** (not from
`litellm_params`) gets a `system_prompt` injected at provider-execution time that the ledger
never sees. This pack records that injection. **INVESTIGATE FIRST** — the right recording
point is an empirical question. Self-contained prompt. No subagents. Commit in your worktree.

## Environment
Worktree: /home/coreyt/projects/airlock/.claude/worktrees/obs-passthrough
Branch: obs-passthrough   Base: 1f400eb
```bash
cd /home/coreyt/projects/airlock/.claude/worktrees/obs-passthrough
git log --oneline -1   # 1f400eb ; git status --short clean
uv run python -c "import airlock; print(airlock.__file__)"   # under the worktree
```
Venv synced. Never cd into the main checkout; never touch main.

## Step 1 — INVESTIGATE (reference: resolve by observation, not assumption)
Trace the enhanced-passthrough flow to find WHERE a config-fallback injection can be
recorded with access to the OUTER request metadata (the dict that becomes
`metadata["airlock_mutations"]` — the same one Site 11 / the other ledger sites append to).
Key files (READ):
- `airlock/guardrails/enhanced_interceptor.py` (Site 11: `EnhancedModelInterceptor`, the
  pre-call hook reads `litellm_params.get("enhanced_profile")`, returns early if absent,
  records the inject at ~`:44`). Does this hook fire — and see `data`/metadata — for the
  config-resolved models too? What does its `litellm_params` contain for such a request?
- `airlock/providers/enhanced_passthrough.py` (`_load_profile_cache` ~`:93`, `_resolve_profile`
  ~`:126` — config-cache fallback when `litellm_params` lacks `enhanced_profile`;
  `_resolve_request` injects `system_prompt` ~`:155-159`; the only metadata there is a fresh
  no-log inner copy — NOT the outer ledger).
Construct a minimal scratch experiment (delete before commit) to observe, for a model whose
config entry defines an `enhanced_profile`: (a) what `litellm_params`/`data`/metadata the
**pre-call** path sees, and (b) whether Site 11 already records it or not. Record the observed
facts. The goal: identify a pre-call point that has the outer metadata AND can detect "this
request will get a config-resolved `system_prompt` injection that Site 11 won't record".

## Step 2 — DECIDE + IMPLEMENT
Likely cleanest fix (confirm with Step 1): in the **pre-call** path that has metadata access
(probably `enhanced_interceptor`'s hook, or wherever the model is known pre-call), when
`litellm_params` lacks `enhanced_profile` but the model resolves to a config-cache profile
with a `system_prompt`, record ONE `record_mutation(metadata, field="system", op="inject",
before=None, after=None, stage="pre_call", source="enhanced.passthrough", reason=<profile
name/marker>)`. Reuse the SAME config-profile resolution the provider uses (the config cache),
don't hand-roll a parallel one if avoidable.
**Hard constraints:**
- Do NOT thread a new parameter through litellm's `CustomLLM.completion(...)` contract or edit
  the inner suppressed call. If the ONLY viable recording point requires that, STOP and report
  — we will defer as a documented known-limitation rather than absorb a contract change.
- CC-T2: `op="inject"` records carry NO content (`after=None`); never put the system-prompt
  text in the record.
- CC-T1/CC-T4: existing `airlock_*` keys and all behavior unchanged; observe-only.
- **No double-record:** a request that goes through Site 11 (litellm_params path) must STILL
  record exactly once; the config-fallback request records exactly once via your new point;
  a non-enhanced request records none.

## You MODIFY
- Most likely `airlock/guardrails/enhanced_interceptor.py` (+ maybe a small read of the config
  cache). Possibly `airlock/providers/enhanced_passthrough.py` ONLY if a metadata-reachable,
  contract-preserving point exists there (prefer the pre-call path).
- Tests: `tests/test_enhanced_interceptor.py` and/or `tests/test_enhanced_passthrough.py`.
You DO NOT TOUCH: `airlock/transparency.py`, the other ledger sites, callbacks, monitor,
`config.yaml`, `pyproject.toml`/`uv.lock`.

## Target Tests (TDD, RED first, no-network)
```bash
uv run python -m pytest tests/test_enhanced_interceptor.py tests/test_enhanced_passthrough.py -q
```
- A config-fallback enhanced request (profile resolved from config, NOT in litellm_params) →
  exactly ONE `op="inject"`, `field="system"`, `source="enhanced.passthrough"` record in
  `metadata["airlock_mutations"]`, value-free.
- A litellm_params enhanced request (Site 11) → still exactly one inject record (no double,
  no regression).
- A non-enhanced request → no inject record.
- Behavior unchanged: the actual injection/target-model resolution is identical to before.

## Cycle: PLAN(memo, incl. your Step-1 observations) → RED → GREEN(≤3) → LINT → COMMIT → CLOSE → REPORT
- COMMIT (scoped, no `-A`): `git commit -m "fix(transparency): OBS-ledger-passthrough — record config-fallback enhanced-profile injection in the ledger"`
  Never `--no-verify`; never push/merge/touch main. Delete any scratch experiment before commit.
- CLOSE: `dev/plans/runs/0.5.0-OBS-ledger-passthrough-output.json` LAST. If you had to escalate
  (contract change required), write it with the blocker and a clear recommendation.
- REPORT: §7 structure incl. `output_json`, and state plainly whether the gap is CLOSED or
  ESCALATED (and why). Console `[OBS-ledger-passthrough][PHASE]` + DETECT/RESOLVE + SUMMARY.

## Definition of Done
The config-fallback enhanced-profile system-prompt injection appends exactly one value-free
`inject` ledger record via a pre-call, metadata-reachable point (no litellm contract change);
no double-record with Site 11; no behavior change; target tests green, lint clean, committed,
output.json written. OR: a clean ESCALATION report if the only fix needs the forbidden
contract change.
