You are an implementing agent for Pack 0.5.7-F4 — stop `enhanced/*` models from
recording $0.00 against real spend.
This prompt is **self-contained** — trust it and the on-disk state, not any memory of
prior conversation. You do **not** spawn subagents. You commit inside your worktree; the
**orchestrator** merges worktree → main.

> ⚠️ **This pack starts with an INVESTIGATION, not a fix.** Step 1 may show there is
> nothing to fix. Do not assume the bug is real until you have measured it. Reaching the
> "cost already survives → regression test only" outcome is a SUCCESS, not a failure.

## Environment

Worktree (your working directory): .claude/worktrees/0.5.7-F4
Branch: 0.5.7-F4
Base commit (fresh from main): 25dadd0 (re-cut from latest `main` at kickoff if newer)

> **Independent** of F-1/F-2/F-3 — different files, may run in parallel.

Verify first:
```bash
cd .claude/worktrees/0.5.7-F4
git rev-parse --show-toplevel
git log --oneline -1
git status --short
uv run --extra test python -m pytest tests/ -q -k "enhanced or cost or spend" 2>&1 | tail -5
```
If older than base, `git merge <base> --ff-only`. If any check fails, STOP and report.

## The bug (claimed)

`enhanced/gemini-coding` really serves `gemini/gemini-3.1-pro-preview-customtools`
($2/$12 per 1M, plus 200K threshold pricing). `enhanced/gemini-coding` is not in
litellm's cost map, so `response_cost` may come back 0/None and record $0.00 — making
budgets, near-limit downgrade routing, and MTD/YTD spend wrong. Aliases:
`gemini-coding`, `aistudio/gemini-coding`.

**Fix the `enhanced/` MECHANISM, not the one model** — any future `enhanced/*` profile
inherits the same defect otherwise.

## STEP 1 — Determine whether the cost actually survives (do this FIRST)

`EnhancedPassthroughHandler.acompletion` returns the **inner** `litellm.acompletion`
response directly (enhanced_passthrough.py:298). That inner call is against a *priced*
model, so its `_hidden_params["response_cost"]` may already be correct and may already
reach Airlock's reader (`response_cost()` at litellm_adapter.py:60-66, read at :63).

Nothing in the Airlock repo strips it between return and read — but litellm's own
CustomLLM cost-tracking may recompute for the OUTER `enhanced/*` model name and zero it.
That is the empirical question.

**Measure it** (no funded key needed): construct or stub an inner response carrying a
known `_hidden_params["response_cost"]`, drive it through the enhanced handler's return
path, and assert what `response_cost()` reads at the adapter. Two outcomes:

- **Cost survives** → write a regression test pinning it, correct
  `dev/notes/ticket-unpriced-models.md` (P1) to say the accounting was already correct,
  update this pack's `output.json` to record the finding, and STOP. No mechanism change.
- **Cost is lost/zeroed** → proceed to Step 2.

Do NOT infer the outcome from the cost map alone — absence from the map explains the
outer model's lack of price, not whether the inner cost propagates.

## STEP 2 — If (and only if) the cost is lost

Preference order — pick the highest that works:

1. **Propagate the inner cost.** The handler knows `target_model` and holds the inner
   response. Re-stamp `_hidden_params["response_cost"]` (and served-model attribution)
   onto the returned object from the inner call. Correct by construction; auto-tracks
   upstream price changes; no table to maintain. **First choice.**
2. **Register the target's pricing for the outer name** via litellm's model-cost
   registration so the outer call prices as the target. Second choice — duplicates a
   price litellm already owns; will drift.
3. **Static `input_cost_per_token`/`output_cost_per_token` in `litellm_params`.** Last
   resort only — hardcodes a price that changes and goes stale exactly like the version
   drift 0.5.6 just fixed. Requires an explicit justification in the design memo if chosen.

Whichever lands, the recorded cost MUST honour the target's **threshold pricing**
(`gemini-3.1-pro-preview*` bills 2x in / 1.5x out above 200K input), not a flat rate —
if you propagate the inner cost, this is automatic; if you register/hardcode, it is not.

## STEP 3 — Decide explicitly (record the decision either way)

- **`tavily/web-search`** bills per search credit, not per token — never in a token cost
  map. Check whether Airlock's cost path honours litellm's `input_cost_per_request` on a
  custom provider. Either wire a flat per-request cost or record why it is deferred.
- **The four self-hosted vLLM models** (`gemma4-31b`, `kimi-dev-72b`, `qwen3-32b`,
  `qwen3.6-27b`) legitimately cost $0 — do NOT fabricate a price. Note whether
  "unpriced by design" should be made distinguishable from "priced-but-unrecorded"; a
  design suggestion in the memo is enough — implementing that distinction is out of scope.

## File Ownership

You MODIFY:
- `airlock/providers/enhanced_passthrough.py`  (only if Step 2 requires it)
- `tests/` — a new `tests/test_enhanced_cost.py` (regression test either way)
- `dev/notes/ticket-unpriced-models.md`         (record the Step 1 finding)

You READ ONLY:
- `airlock/litellm_adapter.py:42-66`  (`hidden_params`, `response_cost`)
- `airlock/fast/monitor.py` (search for `record_spend` — where cost becomes spend)
- `config.yaml` (the `enhanced_profile` blocks — read only, do NOT edit)

You DO NOT TOUCH: `config.yaml`, `pyproject.toml`, `uv.lock`, the guardian/admission code.

## Anchors (re-verified at main = 25dadd0; WILL drift — re-grep before use)

- enhanced_passthrough.py:298-303 — `return await litellm.acompletion(model=target_model, ...)`
- enhanced_passthrough.py:238 — sync twin `return litellm.completion(...)`
- enhanced_passthrough.py:168-172 — inner call sets `no_log=True`,
  `airlock_skip_fathom_logger=True` (suppresses inner logging; does NOT touch cost)
- litellm_adapter.py:42 — `hidden_params()` = `getattr(response, "_hidden_params", None)`
- litellm_adapter.py:60-66 — `response_cost()`; the read is line 63
- monitor.py — `record_spend(now, cost)` (the sink; confirm cost flows here)

## Development Cycle: PLAN → RED → READ → GREEN → LINT → COMMIT → CLOSE → REPORT

Follow `.claude/agents/implementer.md`. **Write `dev/notes/0.5.7-F4-design.md` FIRST**
with the Step 1 result — it decides everything downstream. Commit the memo, then RED,
then GREEN. Run tests as `uv run --extra test python -m pytest …`.

## Definition of Done (the bar the reviewer checks)

- Step 1 result is recorded (memo + `output.json`), and the ticket is corrected to match.
- Either: a regression test pins that `enhanced/*` cost already propagates; **or** a
  request through `gemini-coding` records a **non-zero** cost matching the target rate.
- Long-context (>200K input) records the **surcharged** rate, not the base rate.
- The recorded cost reaches provider-spend accounting, not just the response object.
- Self-hosted vLLM models still record $0 — no fabricated pricing introduced.
- If a fix landed, a second `enhanced/*` profile pointed at a different target would
  price correctly (proven by test) — mechanism, not special-case.
- Step 3 decisions recorded.
- Target tests pass; full no-network suite green (same count as base ± fathomdb skip).
- `output.json` at `dev/plans/runs/0.5.7-F4-output.json`, last.

Commit message (fix path): `fix(0.5.7): attribute enhanced/* cost to the target model (F-4)`
Commit message (no-fix path): `test(0.5.7): pin enhanced/* cost propagation; correct ticket (F-4)`
