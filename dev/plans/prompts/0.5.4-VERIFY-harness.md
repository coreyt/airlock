You are an implementing agent for Pack **0.5.4-VERIFY-harness** — add a consolidated
**cross-sink equivalence harness** proving that ONE `RequestEvent`, dispatched through
the recorder to ALL sinks, reproduces each sink's exact output (the holistic AC-EQUIV).
Self-contained; you commit in your worktree; the orchestrator merges. (The orchestrator
runs the live isolated-instance parity smoke separately — that is NOT your job.)

This is **additive** (a new test file + maybe a tiny harness helper) — no production
code change. The per-sink frozen goldens already exist; this consolidates them into a
single "build once → dispatch many → every sink correct" proof.

## Environment
Worktree only; never cd into the main checkout. Reconcile FIRST:
```bash
git rev-parse --show-toplevel
git merge feat/0.5.4-eventbus --ff-only        # brings the full unified seam (all sinks migrated)
git log --oneline -3                            # expect "MIGRATE-sidechannels CLOSED" / its merge near HEAD
test -f airlock/callbacks/recorder.py && ls tests/fixtures/0.5.4-*-golden.json && echo OK
```
If not a clean ff or toplevel is the main checkout, STOP and report.
Tests: `uv run --extra test --extra sql python -m pytest …`.

## File Ownership
You **CREATE**: `tests/test_cross_sink_equivalence.py`.
You **MODIFY** (only if a tiny shared helper is needed): none expected — prefer
self-contained test code.
You **READ ONLY**: `airlock/callbacks/request_event.py` (`build_request_event`,
`RequestRecorder`, `RequestRecorderCallback`), `airlock/callbacks/projections.py`
(`project_enterprise`/`project_fathom`/`project_s3`/`project_sql`),
`airlock/callbacks/metrics.py` (`record_event`, `_record_mutations_from_event`),
the frozen goldens `tests/fixtures/0.5.4-{entfathom,s3,sql}-golden.json`,
`dev/notes/design-request-event-bus.md` §9.
You **DO NOT TOUCH** any production module or existing test/golden.

## What to build (`tests/test_cross_sink_equivalence.py`) — NO network
A single harness that, for the **representative request set** (reuse the same fixture
inputs the per-sink goldens use — success / provider failure / pre-call-shaped failure /
MCP / Gemini / mutations+served / guardrail keys):
1. **Build once → project many**: build ONE `RequestEvent` per fixture via
   `build_request_event`, then assert ALL FOUR projections from that SAME event match
   their frozen goldens (ignoring only `timestamp`):
   `project_enterprise`/`project_fathom`(env-flag matrix)/`project_s3`/`project_sql`.
   This proves the single sourced event reproduces every sink — the core AC-EQUIV claim
   ("built once, fanned out, each sink identical").
2. **Metrics from the same event**: assert `metrics_callback.record_event(event)`
   produces the expected `requests_total`/`request_duration`/`mutations_total` increments
   for the same fixtures (both success and failure paths).
3. **Cross-sink dispatch via the recorder**: register capturing test-double sinks +
   the real projections with a `RequestRecorder`, dispatch one event, and assert each
   sink received the event and produced its correct record — i.e. one `dispatch()` drives
   all sinks consistently.
4. **Cross-sink failure isolation**: with one sink raising, assert the OTHER sinks still
   produce their correct records and `dispatch` doesn't raise (AC-SEAM, holistic).

Reuse the existing golden fixtures as the oracle (load the JSON); do NOT recapture or
fabricate. If a fixture input helper already exists in `test_projections_equiv.py`,
import/share it rather than duplicating (keep the representative set identical).

## Cycle & DoD
PLAN→RED→READ→GREEN→LINT→COMMIT→CLOSE→REPORT. Memo optional (this is test-only). Commit
scoped files only; never `--no-verify`. Write `dev/plans/runs/0.5.4-VERIFY-harness-output.json`
LAST. Full no-network suite stays green. **DoD:** `tests/test_cross_sink_equivalence.py`
green, proving one `RequestEvent` reproduces all four sink records (vs the frozen goldens)
+ the metrics counters, via the recorder, with cross-sink failure isolation. No production
code touched.
