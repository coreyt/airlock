You are an implementing agent for Pack 0.4.0-B — batch observability (to-do #4 /
design hole A6). Self-contained — trust this prompt and on-disk state. You do NOT
spawn subagents. You commit inside your worktree; the orchestrator merges. Real
implementation following the approved design — do NOT redesign.

## Environment — operate ONLY in this worktree
worktree: /tmp/airlock-0.4.0-B   (orchestrator-created; NOT your default cwd)
branch:   0.4.0-B   (HEAD = current main, includes Pack A's is_batch_call)
output:   /tmp/airlock-0.4.0-B/dev/plans/runs/0.4.0-B-output.json   (write LAST)

Your shell starts in <repo-root> (MAIN checkout). Do ALL work
in the worktree: prefix commands with `cd /tmp/airlock-0.4.0-B &&` and/or
`git -C /tmp/airlock-0.4.0-B ...`; absolute worktree paths for Read/Edit/Write.
NEVER edit/stage/commit under <repo-root>. After each edit
batch, run `git -C <repo-root> status --porcelain -- airlock tests`
— it MUST stay EMPTY (stray-edit tripwire); if not, `[DETECT]`, move into the
worktree, restore main with `git -C <repo-root> checkout -- <f>`, `[RESOLVE]`.

Confirm first:
```
cd /tmp/airlock-0.4.0-B
git rev-parse --show-toplevel        # /tmp/airlock-0.4.0-B
grep -n "def is_batch_call" airlock/guardrails/extract.py   # must exist (Pack A landed)
```
If is_batch_call is missing, STOP and report (wrong base).

BASELINE NOTE (pre-existing on main, NOT yours, do NOT fix): repo-wide ruff flags
`local_vllm_router.py`/`pii_guard.py`/`reasoning_stripper.py` + their tests, and a
Dockerfile spacy-download step. Scope ruff to YOUR files only.

## Required reading
1. `dev/design-unified-batch-gateway.md` (the A6 observability hole + batch record shape).
2. `dev/plans/0.4.0-plan.md` (scope; B is observability only).
3. Code anchors (read, don't over-read):
   - `airlock/callbacks/enterprise_logger.py:199-265` — `_write_log` (rotation +
     `_redact_record`) and `write_precall_block_record` (the public-writer pattern
     to MODEL `write_batch_record` on); `:337-430` `_build_record` (record shape,
     the `call_type`/`mcp_meta` tagging precedent).
   - `airlock/fast/monitor.py:85-138` and `:236-241` — `log_success_event` /
     `log_failure_event`; the `is_mcp = kwargs.get("call_type")=="call_mcp_tool"...`
     guard + `store.record_call_type(is_mcp)`. Mirror with an is_batch guard.
   - `airlock/tui/screens/logs.py:54-56` (type-filter Select options),
     `:211-219` (filter logic: mcp vs llm), `:230-244` (`_populate_table` label).

## Mandate (what B delivers — and does not)
1. **`write_batch_record`** in `enterprise_logger.py`: a public function
   `write_batch_record(*, event, batch_id, provider, model, status, row_count=None,
   input_file_id=None, job_id=None, client=None, error=None)` that builds a
   batch-shaped record **tagged `call_type="batch"` and `is_batch_call=True`**
   (plus the args above, a UTC `timestamp`, and `success` derived from `error`),
   then calls `_write_log(record)` to reuse rotation + redaction. Model it on
   `write_precall_block_record` (structure, `_get_airlock_client` use if
   appropriate, return the record).
2. **monitor exclusion**: in `monitor.py`, exclude batch-tagged events from
   model latency/health/percentile stats so batch doesn't pollute interactive
   metrics — mirror the existing `is_mcp` guard using `is_batch_call` from
   `airlock.guardrails.extract`. (Batch must not feed `get_model(...).record_*`
   latency/health.) Keep client-level accounting as the existing code does for mcp.
3. **TUI tagging** in `logs.py`: add a `"Batch"` option to the type-filter Select;
   in `_apply_filters` add a `batch` branch (`r.get("call_type") == "batch"`) and
   make the existing `llm` branch ALSO exclude batch (so "LLM" view is clean);
   in `_populate_table` show batch rows with a distinct label (e.g. "BATCH").
   Batch records have `model=None` → already coerced elsewhere; keep it null-safe.

**Deferred / DO NOT build:** the gateway/AI Studio adapter (Pack C),
content-scanning, webhooks, PII hydration, Mistral. Do not touch the working
OpenAI/Vertex litellm-native batch paths. Do not touch `pyproject.toml`/`uv.lock`.

## TDD: RED -> READ -> GREEN -> LINT -> COMMIT -> CLOSE -> REPORT
- **RED** (commit tests-only first; record the sha): extend
  `tests/test_enterprise_logger.py` (write_batch_record emits a valid record with
  `call_type="batch"`/`is_batch_call=True` and the passed fields; redaction/rotation
  reused), `tests/test_fast_monitor.py` (a batch-tagged event does NOT update model
  latency/health stats; a normal event still does), and the TUI logs test
  (`tests/test_tui.py` or the logs-screen test — find it) for batch filter +
  batch-tagged record rendering (extend existing null-safety tests). Confirm they
  fail. `echo "[0.4.0-B][RED] tests failing as expected"`.
- **GREEN**: implement 1–3. Follow existing patterns; no unrelated refactors.
  `echo "[0.4.0-B][GREEN] tests pass"`.
- **LINT** (scope to your files): `uv run ruff check <files>` +
  `uv run ruff format --check <files>`. Use `uv run python -m pytest ...` if
  `uv run pytest` lacks deps; `uv sync` is allowed but then you MUST
  `make ensure-spacy` (do NOT skip — it prunes en_core_web_lg).
- **Verify**: `uv run python -m pytest tests/test_enterprise_logger.py tests/test_fast_monitor.py tests/test_tui.py 2>&1 | tail -15` all green, no regression.
- **COMMIT**: `git add` scoped files only (never `-A`/`.`); never `--no-verify`;
  no push/merge/touch main.
- **CLOSE**: write `output.json` LAST to the path above (schema in
  `.claude/agents/implementer.md` §6; `pack`="0.4.0-B", include `tdd_evidence`).

## Console + scope
Emit `[0.4.0-B][PHASE]` lines; `[DETECT]`/`[RESOLVE]` for any surprise; `[SUMMARY]`
at the end (mirror DETECT/RESOLVE into output.json). Stay in-slice: only the three
deliverables + tests. Creep → record in `additional_changes_made_in_scope`, don't
do it. Justified deviation → smallest change, `[DETECT]` loud, escalate if it
touches a shared surface (record schema other loggers depend on, monitor store
API) or another pack.

## Definition of Done
- `write_batch_record` writes a redacted, rotated, batch-tagged record via `_write_log`.
- Batch events excluded from monitor model latency/health stats; normal events unaffected.
- TUI: Batch filter option works; LLM view excludes batch; batch rows labelled
  distinctly; null-safe on `model=None`.
- Your targeted tests green; ruff clean on your files; no network in tests; `output.json` written.
