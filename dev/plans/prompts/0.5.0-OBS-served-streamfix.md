You are an implementing agent for fix-pack 0.5.0-OBS-served-streamfix — a HITL smoke-test
found a real served-by correctness bug on the STREAMING path. For the SAME native Google
AI-Studio backend, `attribute_served_backend` resolves the provider to **`gemini`** on the
non-streaming path but **`vertex_ai_beta`** on the streaming path (litellm's internal
streaming-wrapper provider label leaks through the wrapper attribute that CC-T3 reads on
streams). Streaming and non-streaming must report the SAME served backend. INVESTIGATE FIRST,
then normalize. Self-contained prompt. No subagents. Commit in your worktree.

## Environment
Worktree: /home/coreyt/projects/airlock/.claude/worktrees/obs-served-streamfix
Branch: obs-served-streamfix   Base: 7fb339e
```bash
cd /home/coreyt/projects/airlock/.claude/worktrees/obs-served-streamfix
git log --oneline -1   # 7fb339e ; git status --short clean
uv run python -c "from airlock.transparency import attribute_served_backend, classify_backend_kind; print('ok')"
```
Venv synced. Never cd into the main checkout; never touch main.

## Observed (smoke test, isolated instance)
- Request `gemini-3.5-flash-aistudio` → alias `gemini/gemini-3.5-flash` (Google AI Studio,
  api base `generativelanguage.googleapis.com`).
- Non-streaming response → `X-Airlock-Served-By: gemini` (correct).
- Streaming response → `X-Airlock-Served-By: vertex_ai_beta` (WRONG — same backend).
- The streaming wrapper's `custom_llm_provider` instance attribute carries `vertex_ai_beta`;
  CC-T3 reads that attribute on streams (because `_hidden_params` lacks the provider at
  header-flush time).

## Step 1 — INVESTIGATE (observe, don't assume)
Determine, for a Google AI-Studio gemini model on the STREAMING path, what litellm actually
puts in: (a) the stream wrapper's `custom_llm_provider` instance attribute, and (b)
`_hidden_params["api_base"]` / `model_id` at header-flush time. Read litellm 1.89 source
(`litellm/litellm_core_utils/streaming_handler.py` around the wrapper construction `:113`/
`:156`, and the gemini/vertex handlers) and/or craft a scratch test (delete before commit).
Key question: **is `api_base` on the streaming path `generativelanguage.googleapis.com`
(AI Studio) even though the provider attribute says `vertex_ai_beta`?** That host is the
disambiguator. Record what you observe verbatim.

## Step 2 — FIX (in `airlock/transparency.py` `attribute_served_backend`, additive)
Normalize the served provider so streaming and non-streaming agree:
1. **Alias normalization:** map litellm's `vertex_ai_beta` → `vertex_ai` (it's litellm's
   newer Vertex-Gemini handler name).
2. **AI-Studio-vs-Vertex disambiguation by api_base_host (the design's intended mechanism,
   §3/CC-T3):** when the resolved provider is a Google/Vertex-family value
   (`gemini`/`vertex_ai`/`vertex_ai_beta`) AND `api_base_host` is
   `generativelanguage.googleapis.com` (or otherwise clearly AI Studio per your Step-1
   finding), the served provider is **`gemini`** (native AI Studio). When `api_base_host` is
   a Vertex endpoint (e.g. `*-aiplatform.googleapis.com`), it is **`vertex_ai`** (gateway).
   Use the OBSERVED hosts from Step 1; do not hardcode a host you didn't confirm.
3. Apply this in the provider-resolution path so BOTH streaming (provider from wrapper attr)
   and non-streaming (provider from `_hidden_params`) converge to the same value.
4. **`classify_backend_kind`:** handle `vertex_ai_beta` defensively (→ `gateway`, same as
   `vertex_ai`) in case a raw value slips through.

Keep `ServedBackend` fields + all function signatures unchanged (additive normalization
only). Do NOT change the byte-bound/header serializers. Do NOT touch other files beyond
transparency.py + its tests.

## You MODIFY
- `airlock/transparency.py` (the normalization in `attribute_served_backend` + `classify_backend_kind`).
- `tests/test_served_attribution.py` (and/or `tests/test_header_safety.py` if needed).
You DO NOT TOUCH: callbacks, monitor, config.yaml, pyproject/uv.lock, other source.

## Target Tests (TDD, RED first, no-network — craft SimpleNamespace responses)
```bash
uv run python -m pytest tests/test_served_attribution.py -q
```
- **Streaming AI-Studio:** response with NO `_hidden_params["custom_llm_provider"]` but
  `.custom_llm_provider == "vertex_ai_beta"` and `_hidden_params["api_base"]` =
  `https://generativelanguage.googleapis.com/...` → resolves provider `"gemini"`,
  backend_kind `"native"`. (This is the regression test for the bug.)
- **Streaming Vertex:** `.custom_llm_provider == "vertex_ai_beta"` and api_base a Vertex
  endpoint → provider `"vertex_ai"`, backend_kind `"gateway"`.
- **Non-streaming gemini** (provider `"gemini"` in `_hidden_params`, generativelanguage host)
  → still `"gemini"` (unchanged).
- All EXISTING `test_served_attribution.py` cases stay green (anthropic/bedrock/openai/azure/
  vertex_ai/gemini, the unknown→None path, the streaming wrapper-attr path).
- `classify_backend_kind("vertex_ai_beta") == "gateway"`.

## Cycle: PLAN(memo w/ Step-1 observations) → RED → GREEN(≤3) → LINT → COMMIT → CLOSE → REPORT
- Delete any scratch experiment before commit. COMMIT (scoped, no `-A`):
  `git commit -m "fix(transparency): normalize streaming served-by — vertex_ai_beta + AI-Studio-vs-Vertex by api_base_host"`
  Never `--no-verify`; never push/merge/touch main.
- CLOSE: `dev/plans/runs/0.5.0-OBS-served-streamfix-output.json` LAST. If Step 1 shows the
  api_base is NOT a reliable disambiguator (e.g. litellm uses a vertex host even for AI
  Studio streaming), STOP and ESCALATE with your observations + a recommendation rather than
  guessing.
- REPORT: §7 structure incl. `output_json`, your Step-1 observations, and CLOSED vs ESCALATED.

## Definition of Done
Streaming and non-streaming `attribute_served_backend` report the SAME served provider for
the same backend (AI-Studio gemini ⇒ `gemini`/native; Vertex ⇒ `vertex_ai`/gateway);
`vertex_ai_beta` normalized; `classify_backend_kind` handles it; existing served-attribution
tests green + new streaming regression tests green; lint clean; committed; output.json written.
