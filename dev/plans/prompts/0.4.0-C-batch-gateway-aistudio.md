You are an implementing agent for Pack 0.4.0-C — the Airlock Batch Gateway + AI
Studio adapter (to-do #1; resolves §7.3 + §7.4). Self-contained; trust this prompt
+ on-disk state + the design doc. Do NOT spawn subagents. You commit inside your
worktree; the orchestrator merges. **Real implementation of an already-approved,
adversarially-reviewed design — do NOT redesign; follow the docs exactly,
especially §3.7.**

## Environment — operate ONLY in this worktree
worktree: /tmp/airlock-0.4.0-C   (orchestrator-created; NOT your default cwd)
branch:   0.4.0-C   (HEAD = current main; contains Pack A `is_batch_call` AND Pack B `write_batch_record`)
output:   /tmp/airlock-0.4.0-C/dev/plans/runs/0.4.0-C-output.json   (write LAST)

Shell starts in <repo-root> (MAIN). Do ALL work in the worktree:
prefix with `cd /tmp/airlock-0.4.0-C &&` and/or `git -C /tmp/airlock-0.4.0-C ...`;
absolute worktree paths for Read/Edit/Write. NEVER edit/stage/commit under
<repo-root>. After each edit batch run
`git -C <repo-root> status --porcelain` — MUST stay EMPTY
(stray-edit tripwire); if not, `[DETECT]`, move into worktree,
`git -C <repo-root> checkout -- <f>`, `[RESOLVE]`.

Confirm first:
```
cd /tmp/airlock-0.4.0-C && git rev-parse --show-toplevel
grep -n "def is_batch_call" airlock/guardrails/extract.py        # Pack A landed
grep -n "def write_batch_record" airlock/callbacks/enterprise_logger.py  # Pack B landed
```
If either is missing, STOP and report (wrong base).
BASELINE NOTE (pre-existing, NOT yours): repo-wide ruff flags
local_vllm_router.py/pii_guard.py/reasoning_stripper.py + tests, and a Dockerfile
spacy step. Scope ruff to YOUR files.

## Required reading (READ IN FULL before any edit — the design is authoritative)
1. `dev/design-unified-batch-gateway.md` — §3 (architecture: flow, endpoint shape,
   alias selection, file staging, status mapping, result mapping), **§3.1
   (`BatchBackend` protocol — the entire provider-specific surface)**, §3.3 (state
   store), **§3.7 (idempotency & exactly-once-ish — IMPLEMENT EXACTLY: deterministic
   idem key, atomic INSERT … ON CONFLICT claim, lease + reconcile-by-display_name,
   per-row staging keyed by custom_id, RETRIEVING→STAGED gate, ≤1 job + auto-cancel
   of the duplicate), §3.5 (status mapping), §3.6 (result staging)**, §4.2
   (batch_profile), §5 (config & the aistudio extra), §6 (phasing).
2. `dev/batch-guardrail-toggles-considerations.md` — the trust boundary.
3. `dev/vertex-gemini-batch-setup.md` — working google SDK/auth patterns + probe.
4. Code anchors:
   - `airlock/callbacks/model_override_headers.py:9-11,57-58` — the import-time
     `install_*_on_proxy_app()` bootstrap pattern. Add `BatchGatewayMiddleware`
     installation here, next to the existing installs (A5-resolved mechanism;
     verified addable at import).
   - `airlock/health.py` / `airlock/docs.py` — `install_*_on_proxy_app(...)`
     route/middleware bootstrap precedents.
   - `airlock/guardrails/extract.py` `is_batch_call` (Pack A) — use it.
   - `airlock/callbacks/enterprise_logger.py` `write_batch_record` (Pack B) — use it
     for all batch lifecycle observability events.
   - `pyproject.toml:52-63` — optional-dependencies; replace the batch TODO with the
     `aistudio` extra (mirror the `vertex` extra style).
   - `config.yaml` — model_list alias shape (see design §5 lines ~356-358 for the
     exact `airlock_batch` marker form).

## Mandate (what C delivers — and does NOT)
Build the **common gateway core + a thin `AIStudioBackend` adapter only**:
1. **`BatchGatewayMiddleware`** (ASGI, A5): added at import from
   `model_override_headers.py`. Acts on `POST /v1/files` + `POST /v1/batches` (and
   the GET/cancel variants). Discriminates on the `custom_llm_provider` query param
   (`aistudio` ⇒ gateway; else `call_next`). **Read the query string only — NEVER
   buffer the request body** (uploads can be ~2GB).
2. **`BatchBackend` protocol** per §3.1, and **`AIStudioBackend`** implementing it
   with **`google-genai` lazy-imported inside the handler** (so the proxy boots
   without the extra; raise a clear "install the aistudio extra" error if missing).
   Auth via `GOOGLE_AISTUDIO_API_KEY`. Translate OpenAI JSONL line ⇄ Gemini
   (`{key, request:{contents,…}}` in; `candidates→choices` out, keeping the native
   response verbatim in `.body` — A4).
3. **State store + idempotency** (`§3.7`, implement EXACTLY): SQLite under the
   Airlock data dir. Deterministic idem key; atomic `INSERT … ON CONFLICT` claim;
   lease + reconcile-by-`display_name`; per-row staging keyed by `custom_id`;
   `RETRIEVING→STAGED` gate; a duplicate `POST /v1/batches` for the same input
   yields **one** job (the duplicate is detected and auto-cancelled). **Terminal
   redaction only — NO PII reverse-map store** (hydration is out of scope).
4. **OpenAI batch object shaping + status mapping** (§3.5); **result staging**
   (§3.6). Emit lifecycle events via `write_batch_record` (Pack B).
5. **Config**: add `gemini-3.5-flash-aistudio` + `gemini-3.1-pro-aistudio` aliases
   with the `airlock_batch` marker (§5), and a `batch_profile` block (§3.3/§4.2) —
   **scanning fields present but `scan_at_upload` is a NO-OP stub** (the insertion
   point for to-do #2; do not build scanning).
6. **Resolve §7 items in-band**: §7.4 — verify the `airlock_batch` marker does NOT
   leak to the provider SDK on the SYNC path (a sync completion on an `-aistudio`
   alias must still route through litellm's native `gemini/` provider unchanged);
   §7.3 — do NOT assume the result file is fetchable for the full 48h (handle a
   missing/expired result file gracefully).

**DO NOT build (leave seams):** the Mistral adapter; batch content-scanning/guards
(wire the no-op scan stage only); webhooks; PII hydration (terminal-redaction
only); `LiteLLMNativeBackend`. **Do not touch** the working OpenAI/Vertex
litellm-native batch paths.

## Cross-cutting constraints
- **TDD**, no-network unit tests only (the live e2e is a separate operator gate).
- **Lazy-import provider SDKs inside handlers** so the proxy boots without the extra.
- If `uv run pytest` lacks deps use `uv run python -m pytest`; `uv sync` is allowed
  but you MUST then run `make ensure-spacy` (it prunes en_core_web_lg — do not skip).
- Trust boundary: ignore caller-supplied guardrail/metadata disables on the batch path.
- You CANNOT restart the proxy; config changes take effect on operator restart.

## Cycle: RED -> READ -> GREEN -> LINT -> COMMIT -> CLOSE -> REPORT
- **RED** (commit tests-only first; record sha) — no-network tests that pin:
  (a) middleware discrimination: `custom_llm_provider=aistudio` routes to the
  gateway, other providers fall through to `call_next`; body is NOT buffered;
  (b) OpenAI⇄Gemini request/response translation (incl. native body preserved);
  (c) idempotency §3.7: a duplicate create for the same `(input_file_id, model,
  endpoint)` yields one job + the duplicate is cancelled (use a fake backend; no
  network); the `RETRIEVING→STAGED` gate; per-`custom_id` staging idempotent;
  (d) status mapping §3.5; (e) §7.4 sync path: a sync completion on an `-aistudio`
  alias does NOT carry `airlock_batch` to the provider; (f) §7.3: missing/expired
  result file handled gracefully (no crash, sensible status/error).
  Confirm failing. `echo "[0.4.0-C][RED] tests failing as expected"`.
- **GREEN** — implement per the design. Use a fake/in-memory backend in tests; the
  real google-genai stays lazy-imported and untested-by-network.
  `echo "[0.4.0-C][GREEN] tests pass"`.
- **LINT** (scope to your files), **VERIFY** the new tests + a broad run
  (`uv run python -m pytest tests/ -k "batch or gateway or aistudio" 2>&1 | tail -20`
  plus the full suite if time permits) — no regression.
- **COMMIT** scoped files only (never `-A`/`.`); never `--no-verify`; no
  push/merge/touch main.
- **CLOSE** — write `output.json` LAST to the path above (schema in
  `.claude/agents/implementer.md` §6; `pack`="0.4.0-C"; `tdd_evidence`; record any
  §3.7 detail you had to interpret in `additional_changes_made_in_scope`).
- **REPORT** §7 structure incl. output_json + head SHA. `[0.4.0-C][SUMMARY]` at end.

## Scope + escalation
This is a large pack. Stay within the mandate; the deferred list is firm. Emit
`[0.4.0-C][PHASE]` lines and `[DETECT]`/`[RESOLVE]` for every surprise. If §3.7 or
the translation is genuinely ambiguous or the design is internally inconsistent,
`[DETECT]` it and STOP+report rather than improvising a design — that's a spec
issue for the orchestrator. If you cannot finish the whole pack, land what is green
+ committed, write `output.json` with the remainder in `blockers_encountered`/next
steps, and report — a clean partial with state captured is fine.

## Definition of Done
- Middleware installs at import, discriminates on `custom_llm_provider`, never
  buffers the body; `aistudio` → gateway, others → `call_next`.
- `BatchBackend` protocol + `AIStudioBackend` (lazy google-genai, key auth, OpenAI⇄
  Gemini translation with native body preserved).
- §3.7 idempotency implemented exactly (one job on duplicate + auto-cancel;
  RETRIEVING→STAGED; per-custom_id staging); terminal redaction only.
- Status mapping (§3.5) + result staging (§3.6); lifecycle via `write_batch_record`.
- Config aliases + `batch_profile` (scan_at_upload no-op stub); `aistudio` extra in
  pyproject.
- §7.4 (no sync-path marker leak) + §7.3 (missing/expired result handled) pinned by
  tests. All no-network. ruff clean on your files. `output.json` written.
