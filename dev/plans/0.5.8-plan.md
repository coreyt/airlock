# Airlock 0.5.8 — Plan

**Status: 🟡 CONFIG-APPLICABILITY UX AND RELEASE HYGIENE REMAIN — RELEASE PREPARATION
AFTER P-8/P-9.** On
2026-08-02 the owner authorized P-2 and P-2b enforcement based on the completed
deterministic test and independent-review evidence. The previous calendar measurement
windows remain useful post-release operational monitoring, not release blockers. The
direct OpenAI `max` probe is wired and was attempted but no funded key was available;
current official OpenAI GPT-5.6 guidance explicitly supports `max`, so it is accepted
for that family model-specifically. See
[`../notes/0.5.8-enforcement-design.md`](../notes/0.5.8-enforcement-design.md).

Release theme: **enforcement and configuration clarity** — turn the warn-only
detection shipped in 0.5.6 into real rejections, then make the TUI explicit about
which configuration changes take effect immediately and which require a proxy restart.

> **AUTHORITATIVE CURRENT SCOPE — 2026-08-02.** The detailed sections below retain
> their design and measurement provenance. Where they conflict with this table or the
> release checklist, this table and checklist win.
>
> | item | status |
> |---|---|
> | P-2 reasoning-effort rejection | ✅ implemented — known-invalid OpenAI/Azure values receive an actionable 400; `AIRLOCK_VALIDATE_REASONING_EFFORT=0` is the rollback switch |
> | P-2b cross-tier fuzzy resolution | ✅ implemented — cross-tier matches receive a 404 plus an eligible suggestion; same-tier typo tolerance remains |
> | P-6 model-alias disclosure | ✅ completed 2026-08-01 |
> | P-6a / P-6b inspections | ✅ completed — no code change required |
> | P-7 current-behavior docs | ✅ completed |
> | P-8 config applicability (#34) | ✅ implemented — focused ConfigPane suite green |
> | **P-9 release hygiene and issue audit** | 🟡 **required closeout item** |
> | Other open GitHub issues | ⏸ deferred; see “Deferred backlog” below |
>
> **P-6 was a real production gap, not a nice-to-have.** 0.5.6 advanced
> `gemini-flash-lite`, `gemini-pro` and `gemini-flash` to new generations **without**
> the `served=` disclosure that was supposed to make such advances visible. The
> 2026-08-01 implementation adds the disclosure for these configured aliases.

Design doc: [`dev/design-gpt-5.6-model-family.md`](../design-gpt-5.6-model-family.md).
Read it first — this plan does not restate the rationale.

**In scope:** completed P-2/P-2b/P-6/P-6a/P-6b/P-7, P-8, P-9, and no additional
product features.

**Deferred (deliberate):** batch endpoint support for 5.6 (§8.3 of the design —
needs an exercised round-trip first); retiring `gpt-5.4`; deduplicating
`_DEFAULT_COST_TIERS` into a shared constant; context-aware fallback selection;
the `_strip_version` alpha-suffix behaviour (pre-existing, not a regression); broader
TUI catch-up packs; provider authorization; search; virtual-key management; telemetry
status; semantic-classifier detail; and MCP startup-timeout work.

**Ordering constraint:** the 0.5.6 hotfix and the 0.5.7 admission-gate work both ship
before this release. Nothing here is gated on them except that P-4's config edits land
on top of the 0.5.6 corrections.

---

## P-0 — ✅ EXTRACTED to 0.5.6

The urgent template/config hotfix moved to its own release:
[`dev/plans/0.5.6-plan.md`](0.5.6-plan.md). **Already implemented** (branch
`fix/model-config-hardening`, suite green) and must ship before 2026-07-23, when
`gpt-5.1-codex` stops serving API requests.

Release order: **0.5.6** hotfix → **0.5.7** admission gate → **0.5.8** this plan.

## P-1 — ✅ RESOLVED (no longer a blocker)

**Outcome: Airlock does NOT under-count. No fix required.** See design §12.

- Airlock never computes cost — single source is litellm `response_cost`
  (`litellm_adapter.py:60`); no independent derivation exists anywhere.
- litellm **does** honour the threshold fields
  (`llm_cost_calc/utils.py:216-333`), measured on input tokens only.
- No pre-flight cost estimate exists; budget gating reads recorded actual spend.

**The 272K surcharge IS real and litellm IS correct** (design §12.4). An interim
revision of this plan claimed 5.6 had dropped the surcharge and that litellm
over-counts — **that was wrong and is withdrawn.** litellm's day-0 post publishes
explicit short/long dual rates per variant (sol $5/$10 in, $30/$45 out; terra
$2.50/$5, $15/$22.50; luna $1/$2, $6/$9). **No override, no upstream bug, no work.**

**One finding remains:**

- [ ] **Separate ticket:** 7 configured models are absent from litellm's map and
      record **$0.00** (design §12.6). Pre-existing, unrelated to 5.6.

<details><summary>Original blocker text</summary>

**Status:** blocks P-4 sign-off. Do this **before** writing 5.6 config entries.

**Context:** GPT-5.6 bills **2× input / 1.5× output** above 272K input tokens. litellm
carries `*_above_272k_tokens` fields in the model map. Design §11 Q1: it is
**unverified** whether Airlock's spend accounting reads them.

**What needs to happen:**
- Trace the cost path from litellm `response_cost` through `airlock/litellm_adapter.py:60`
  into `SpendStore` / provider-spend accounting.
- Determine whether a >272K-input request records the surcharged cost or the base rate.
- If it under-counts: this is a **budget-correctness bug** affecting exactly the
  long-context requests where 5.6 is most attractive. Decide whether to fix in 0.5.7 or
  document and defer — but decide explicitly, do not discover it in production.

### Exit criteria

A written answer in the design doc's §11, and either a fix or a recorded deferral
with rationale.

</details>

---

## P-2 — ✅ IMPLEMENTED — `reasoning_effort`: validate and reject, stop translating

> **Resolution — 2026-08-02:** the owner waived the calendar gate after deterministic
> and independent-review evidence established the behavior and reporting contracts.
> The warn-only window remains post-release monitoring, not a release blocker.
> `max` is accepted for the GPT-5.6 family because current official OpenAI guidance
> documents it; the direct funded-key probe remains a useful operational check. Known
> invalid OpenAI/Azure values now receive an actionable 400, with
> `AIRLOCK_VALIDATE_REASONING_EFFORT=0` as the short-term rollback switch.


**Status:** complete. The shipped behavior rejects known-invalid OpenAI/Azure values
without rewriting valid client intent. See design §5.

**Independence caveat:** P-2 is independent of P-3/P-4 **only if its tests mock
`litellm.get_model_info` and inject a synthetic `model_list` fixture.** Written as
integration tests against real config, they would depend on 5.6 entries that do not
exist until P-4 and on model-map behaviour that is not deterministic until P-3. Write
them mocked, or move the 5.6-specific assertions after P-4. Do not pretend the
dependency isn't there.

**Files:** `airlock/reasoning_effort.py` (call site: `airlock/fast/guardian.py:499`).

**Decision (design §5.1): Airlock stops rewriting client intent.** Valid values pass
through untouched; invalid ones are **rejected with an actionable 400**. No synonym
mapping — `highest` is not accepted as `max`. This turns a class of silent
wrong-behaviour bugs into loud, immediate, correct errors.

> **Historical rollout note:** P-6c was intentionally warn-only first. The enforcing
> behavior described here now ships; retain its events and reports for operational
> monitoring rather than treating them as an unclosed release gate.

### Changes

- Add `_supported_efforts(model) -> frozenset[str] | None` per design §5.2, built from
  the litellm map's per-level capability flags. Never hardcode the level set.
- **Resolve the Airlock alias to its `litellm_params.model` body first**, then consult
  `litellm.get_model_info`. `data["model"]` may be a semantic alias (`gpt-5`) whose
  body lives in `model_list`; prefix-stripping alone is insufficient. *(Gap codex
  review found in the original proposal — do not skip it.)*
- Unknown model → **pass through untouched**. Do not reject on ignorance; self-hosted
  and custom endpoints legitimately aren't in the map (design §12.6 lists seven).
- **Delete** `_OFF_INTENT`, `_OPENAI_VALID`, `_GEMINI_VALID` and every translation
  branch — OpenAI, Gemini, and Anthropic alike. They encode the guesses §5.1 forbids.
- Decide `max` deliberately (reject vs allow-list pending upstream) — it must not
  survive by fall-through accident as it does today.
- Remove the success-path `record_mutation` calls; with no rewrite there is no
  mutation to record.
- Rename `AIRLOCK_NORMALIZE_REASONING_EFFORT` to reflect that it now gates
  **validation**, and keep it as an escape hatch to fall back to `drop_params`.
- Rewrite the module docstring — its stated premise is now inverted.

### Tests (TDD — write these first)

The assertions below describe the shipped enforcing behavior. The earlier warn-only
assertions remain regression coverage for the observability/reporting path.

- [ ] `none` passes through **untouched** for `gpt-5.6-*` (today: rewritten to
      `minimal`, then dropped, yielding max reasoning).
- [ ] `minimal` is **rejected with 400** for `gpt-5.6-*` (the model rejects it).
- [ ] `minimal` passes through untouched for `gpt-5.4`.
- [ ] `none` is **rejected with 400** for `gpt-5.4` — the breaking change; assert it
      deliberately so it can't regress silently back to translation.
- [ ] `xhigh` passes through for `gpt-5.6-*`.
- [ ] `highest` is **rejected**, never mapped to `max` or `high`.
- [ ] Error body names the rejected value, the resolved model, and the supported set.
- [ ] Unknown/unresolvable model → value untouched, **no rejection**, no exception.
- [ ] Semantic alias (`gpt-5`) resolves through `model_list` to the right body.
- [ ] Gemini: `disable` passes through; a bogus level is rejected, not translated.
- [ ] Anthropic: values pass through; nothing is dropped or invented.
- [ ] Escape-hatch env var disables validation entirely.
- [ ] **No mutation-ledger entry** is emitted on the pass-through path.

### Exit criteria

**Shipped enforcement:** a client asking for `none` on a 5.6 model gets
`none`. A client asking for something the model rejects gets a 400 telling them exactly
what to send instead. Airlock never substitutes a value other than the one requested.

### Release notes (breaking)

Clients sending `none`/`off` to older OpenAI models now receive a 400 where they
previously got a silently-translated `minimal`. This is intentional — the translation
was invisible to them and produced wrong results — but it **must** appear in the
changelog and release notes.

---

## P-2b — ⚠️ SPLIT — Strict model resolution

**Shipped in 0.5.6:** the dropped-qualifier guard. `resolve()` refuses any fuzzy match
that would ignore a non-numeric token the caller supplied, and `suggest()` returns
ranked candidates with cost tier. Zero blast radius — the refused names
(`gpt-5.6-mini` etc.) only became reachable when 5.6 was added.

**Moved to 0.5.7:** wiring the *helpful response*. Today a refusal leaves the unknown
name in `data["model"]` and litellm returns its own generic error, so the caller gets
**no suggestion at all** despite `suggest()` already computing one. Unblocked and
additive — it changes the error body, not what is refused.

**Implemented here:** extending refusal to *pre-existing* aliases when a close fuzzy
match crosses a configured cost tier. On 2026-08-02 the owner waived the preceding
calendar window as a release gate after the real enterprise-JSONL/query/report
contract and independent review passed. The instrumentation/runbook remains useful
post-release monitoring; do not infer behavior from the dropped-qualifier guard alone.

<details><summary>Original combined item</summary>


**Status:** new; design §4.2. Replaces the withdrawn guess-guard approach.

**Files:** `airlock/fast/model_alias.py`, `airlock/proxy_errors.py`,
`airlock/transparency.py`, `airlock/callbacks/model_override_headers.py`.

Sibling of P-2: same principle (**don't substitute silently**), applied to model names
instead of `reasoning_effort`. Ships behind the same warn-only window (P-6c).

### Enforcing changes (after its separate measurement gate)

- Fuzzy matching becomes a **diagnostic**, not a router:
  - exact match → serve;
  - near-match **within the same cost tier** → serve, log the resolution (keeps typo
    tolerance and dated snapshots like `gpt-5.6-sol-2026-07-09` working);
  - near-match **crossing a cost tier** → **reject**, using the score to *suggest*;
  - nothing above `_WARN_THRESHOLD` → reject, list available models.
- **404 + `code: model_not_found`**, OpenAI-shaped so existing SDK error paths work.
- `error.message` must be **self-sufficient prose** — most clients surface only that.
  The structured `error.airlock.suggestions` block (model, score, tier, price) is a
  bonus, not the payload.
- Add `X-Airlock-Model-Suggestion` using the established `;`-joined `key=value`
  grammar — reuse the `_mutation_token` serializer, do not write a second one.
- Suggestions are **derived** from the existing scorer + tier tables, never hardcoded.
- **Never suggest a model the caller is not entitled to** — respect per-client catalog
  filtering. A helpful error must not become a capability-disclosure channel.
- Threshold constants keep their values, change meaning: 0.50 now gates *same-tier
  auto-routing*; 0.35–0.50 becomes *suggestion* territory.

### Tests

- [ ] `gpt-5.6-mini` → 404, suggests `gpt-5.6-luna`, `reason=fuzzy_match_crosses_cost_tier`.
- [ ] `gpt-5.6-sol-2026-07-09` → still auto-routes (same tier); no rejection.
- [ ] A same-tier typo still auto-routes — the convenience feature is not lost.
- [ ] `error.message` alone contains the suggestion and the available models.
- [ ] `X-Airlock-Model-Suggestion` grammar matches `_mutation_token` byte-for-byte.
- [ ] Suggestions never include a model outside the caller's permitted catalog.
- [ ] Warn-only mode: identical routing to today, but a structured WARNING event and
      ledger record that are queryable from the normal RequestEvent path.

### Exit criteria

A client that guesses a non-existent model name is told exactly what to send instead,
and is never silently billed for a model it did not ask for.

---

</details>

---

## P-3 — ✅ SHIPPED IN 0.5.6 — litellm floor bump

**Files:** `pyproject.toml:44`.

```toml
"litellm[proxy]>=1.93.0,!=1.82.7,!=1.82.8,<2",
```

Rationale is **determinism, not capability** (design §7): 1.89.0 works only because
litellm fetches the remote map at import. Under `LITELLM_LOCAL_MODEL_COST_MAP=True`,
air-gapped deploys, or a fetch failure, 5.6 degrades to unknown-model and cost tracking
reads **$0.00**.

The two `!=` exclusions fall below the new floor and are redundant; dropping them is
optional tidying.

### Tests

- [ ] Upgrade the venv; full suite green on 1.93.0.
- [ ] New: assert every `model_list` target resolves in `litellm.model_cost` with a
      non-zero input cost, run with `LITELLM_LOCAL_MODEL_COST_MAP=True` so it catches
      exactly the bundled-map gap this bump exists to close.

### Exit criteria

Suite green on 1.93.0; no shipped model prices at $0.00 with the local cost map forced.

---

## P-4 — ✅ SHIPPED IN 0.5.6 — GPT-5.6 model entries

**Files:** `config.yaml`, `airlock/cli/templates/config.yaml`.

**Depends on:** P-1 resolved, P-3 merged.

### Changes

Insert after the existing OpenAI bare block (~line 112). **Order is load-bearing**:
`-sol` must precede bare `gpt-5.6` so dated snapshots resolve to the explicit variant.

Entries (all with `api_key: os.environ/OPENAI_API_KEY`):

| `model_name` | `litellm_params.model` | note |
|---|---|---|
| `gpt-5.6-sol` | `openai/gpt-5.6-sol` | real |
| `gpt-5.6-terra` | `openai/gpt-5.6-terra` | real |
| `gpt-5.6-luna` | `openai/gpt-5.6-luna` | real |
| `gpt-5.6` | `openai/gpt-5.6-sol` | family alias, pinned to explicit body |
| `openai/gpt-5.6-sol` | `openai/gpt-5.6-sol` | twin — **load-bearing**, see §4.1 |
| `openai/gpt-5.6-terra` | `openai/gpt-5.6-terra` | twin |
| `openai/gpt-5.6-luna` | `openai/gpt-5.6-luna` | twin |
| `openai/gpt-5.6` | `openai/gpt-5.6-sol` | twin |

**Eight entries — no synthetic ones.** The `gpt-5.6-mini`/`-nano`/`-pro` guess-guard
aliases from earlier drafts are **dropped** (design §4.2): those names are handled by
strict resolution with a helpful 404, not by inventing models OpenAI never shipped.

**Comment requirements** (these carry the design's reasoning into the config, where the
next reader will be):
- The family is a flat price-tier triple over one capability envelope — **not** a
  mini/nano capability ladder. Luna is **not** a nano replacement ($1/$6 vs $0.20/$1.25).
- `>272K` input tokens = 2× input, 1.5× output surcharge.
- `-sol` precedes bare `gpt-5.6` **deliberately** — dated-snapshot resolution.
- The twins are **load-bearing**, not cosmetic (`airlock/fast/model_alias.py:287` setdefault is
  first-writer-wins).
- **No `airlock_batch` marker** — deliberate; see design §8.3.
- The 272K long-context surcharge (2x in / 1.5x out, whole request) **is real** — say
  so. An earlier draft wrongly claimed 5.6 dropped it; see design §12.4.

**⚠️ Bare `gpt-5.6` is NOT a real OpenAI id** (design §2.2, verified live against
`/v1/models`). The alias must stay pinned to the explicit `openai/gpt-5.6-sol` body
and that string must never be forwarded upstream. Assert this in tests — pointing it
at the floating bare id would 404 every request through the alias.

**Legacy aliases are NOT touched.** `gpt-5`→5.5, `gpt-5-pro`→5.5-pro, `gpt-5.4`→5.4,
mini/nano→5.4-*, codex→5.3-codex all stay. Add the `config.yaml:51` comment stating the
`-pro` contract: it denotes the deep-reasoning/`responses` tier and may lag the
flagship's version number when OpenAI has not shipped a pro for the current line.

**Template:** mirror the bare entries **and the twins** (design §4.1 — omitting twins
would leave new installs exposed to the same misroute).

Also add a comment at `airlock/fast/model_alias.py:287` warning that the `setdefault` is
first-writer-wins and that aliases sharing a body need explicit prefixed twins.

### Tests

- [ ] `openai/gpt-5.6-sol` resolves to `openai/gpt-5.6-sol` — **not** `gpt-5` or `gpt-5.6`.
      This is the regression the twins exist to prevent; assert it explicitly.
- [ ] `gpt-5.6-sol-2026-07-09` (dated snapshot) resolves to `gpt-5.6-sol` — proves the
      ordering constraint holds.
- [ ] `GPT-5.6-SOL` resolves case-insensitively.
- [ ] `gpt-5.6-mini` / `-nano` / `-pro` are **rejected with a 404** carrying a
      `did you mean` suggestion, NOT silently routed (design §4.2).
- [ ] The rejection names a same-tier suggestion and includes the `airlock.suggestions`
      block and the `X-Airlock-Model-Suggestion` header.
- [ ] Bare `gpt-5.6` resolves to the `openai/gpt-5.6-sol` body — the literal string
      `gpt-5.6` is never sent upstream.
- [ ] Bare `sol` / `terra` / `luna` resolve to `None` (below threshold — correct).
- [ ] `test_config_consistency.py` green on root **and** template (shipped in 0.5.6).
- [ ] Capability records: `endpoints == ["chat"]` for all 5.6 entries; `deprecated` is
      `False` for all four real names.
- [ ] Every legacy alias still resolves to its **pre-change** target — a pinned
      snapshot test, so a future edit cannot silently repoint `gpt-5` or `gpt-5-nano`.
- [ ] `/v1/models` lists only real models — no synthetic entries exist to leak.

### Exit criteria

All 5.6 names resolve correctly; no legacy alias changed; capability records claim
`chat` only.

---

## P-5 — ✅ SHIPPED IN 0.5.6 — Cost tiers and fallbacks

**Files:** `config.yaml` (`cost_tiers:` ~643, `fallbacks:` ~608), plus
`airlock/fast/router.py:44-66` **and** `airlock/fast/settings.py:64-88`.

### Changes

> ⚠️ **Read design §8.1.1 first.** `_apply_cost_tier` (`airlock/fast/router.py` (`_apply_cost_tier`))
> does a plain `if model in tier_models` membership test and **force-swaps to
> `tier_models[0]` on a miss**. An untiered alias is not left alone — it is silently
> rerouted to a different model. P-4 creates **eight** callable names; tiering only
> the three bare variants would leave five subject to forced swaps.

Tiers — every callable alias must appear in exactly one tier:

| tier | entries to add |
|---|---|
| high | `gpt-5.6-sol`, `openai/gpt-5.6-sol`, `gpt-5.6`, `openai/gpt-5.6` |
| medium | `gpt-5.6-terra`, `openai/gpt-5.6-terra` |
| low | `gpt-5.6-luna`, `openai/gpt-5.6-luna` |

Place each **after** the incumbent leaders so the tier's first entry — the swap
target — is unchanged (additive). Luna is the **ceiling** of `low`; `gpt-5-nano` stays
the floor and stays listed first.

Fallbacks — **all eight callable names need a chain** (design §8.2 has the concrete
block). A name with no chain has no failover at all. Chains are grouped by **body** so
aliases sharing a body fail over identically:

| body group | chain |
|---|---|
| sol: `gpt-5.6-sol`, `openai/gpt-5.6-sol`, `gpt-5.6`, `openai/gpt-5.6` | `[gpt-5.6-terra, gpt-5.6-luna, claude-opus]` |
| terra: `gpt-5.6-terra`, `openai/gpt-5.6-terra` | `[gpt-5.6-luna, gpt-5.6-sol, claude-sonnet]` |
| luna: `gpt-5.6-luna`, `openai/gpt-5.6-luna` | `[gpt-5.6-terra, gemini-flash, claude-sonnet]` |

Luna deliberately does **not** fall back to `claude-haiku` — see below.

> ⚠️ `_DEFAULT_COST_TIERS` is duplicated byte-for-byte in `router.py` and `settings.py`.
> **Update both** or the fast path and settings path disagree.

Fallbacks — per design §8.2. Luna deliberately does **not** fall back to `claude-haiku`:
haiku caps at 200K vs 5.6's 1.05M, so a long-context failover would **hard-fail** rather
than degrade.

### Tests

- [ ] **Every callable alias created in P-4 appears in exactly one tier.** Enumerate
      `model_list` and assert tier membership — this is the forced-swap guard, and it
      must be a completeness assertion, not eight hand-written cases.
- [ ] For each of the 8 names: `_apply_cost_tier(<its tier>, <name>)` returns the
      **same** name with `reason is None` — i.e. no forced swap.
- [ ] Tier lookups agree between `router.py` and `settings.py` for all entries —
      a direct anti-drift assertion on the duplicated dicts.
- [ ] **Every callable alias created in P-4 is a fallback *source*.** A completeness
      assertion, not eight hand-written cases — this is the sibling of the tier guard
      and catches a name shipped with no failover.
- [ ] Aliases sharing a body have **identical** chains (`openai/gpt-5.6-sol` fails over
      the same as `gpt-5.6-sol`).
- [ ] Every 5.6 fallback target exists in `model_list` (covered by the 0.5.6 test extension).
- [ ] No 5.6 fallback target has a context window below 1,000,000 — encodes the
      hazard so a future edit cannot reintroduce a haiku-shaped failover.
- [ ] `gpt-5-nano` remains the first entry in `low`.

### Exit criteria

Tiers consistent across both dicts; no fallback target can hard-fail on context.

---

## P-6 — `X-Airlock-Model-Alias` response header

> ⚠️ **Now covers a live gap.** 0.5.6 advanced `gemini-flash-lite` -> 3.1-flash-lite and
> `gemini-pro`/`gemini-flash` to the 3.x line. The `served=` disclosure that was
> supposed to make such advances visible does not exist yet, so callers are on
> different models — at different prices — and have not been told. This was accepted
> knowingly to hit the 2026-07-23 deadline; it should not stay unshipped.


**Status:** ✅ completed 2026-08-01; see design §6.

**Files:** `airlock/transparency.py`, `airlock/callbacks/model_override_headers.py`,
**plus** `config.yaml` + `airlock/cli/templates/config.yaml` (the successor map),
`airlock/fast/settings.py` or `router.py` (map loading/validation, following the
`cost_tiers` pattern), `tests/test_config_consistency.py` (map validation), and docs.

**Depends on:** P-4 (successor targets must exist in `model_list`) **and** the
successor-map config plumbing below — the original "depends on P-4 only" was wrong.

### Changes

Emit on OpenAI-family responses:

```
X-Airlock-Model-Alias: requested=gpt-5;served=openai/gpt-5.5;newer=openai/gpt-5.6-sol
```

**Grammar:** `;`-joined `key=value`, **no space after the separator**, each value via
`_header_safe`, byte-budgeted with the same `…+N more` truncation as
`mutations_header`. This matches `_mutation_token` exactly — **reuse the existing
serializer, do not write a second one.**

**Successor map** (design §6.1) — new optional `model_successors:` block in
`config.yaml`, `dict[str, str]` mapping **requested alias → advertised successor
alias**, loaded/validated on the `cost_tiers` pattern (env → config → empty default):

```yaml
model_successors:
  gpt-5: openai/gpt-5.6-sol
```

Keys and values must both exist in `model_list`, enforced in
`test_config_consistency.py` the same way fallback sources/targets are. Default empty:
with no map configured, the header is never emitted.

> ⚠️ **Look up the key using `metadata["airlock_request"]["requested_model"]`, not
> `data["model"]`.** The guardian already preserves the original request. By post-call
> time `data["model"]` may have been rewritten by routing, a cost-tier swap, or
> failover — keying off it would advertise a successor to a model the client never
> asked for. Load the map via an `AirlockSettings.model_successors` field following the
> `cost_tiers` loader at `airlock/fast/settings.py:209`.

Constraints:
- **Additive and advisory only — must never change routing.**
- Emitted through the existing `AirlockModelOverrideHeaders` post-call hook, gated by
  the transparency config, consistent with `served_headers` / `mutations_header`.
- `newer=` is **data-driven from a successor map**, not a hardcoded string, so it does
  not rot on the next release.
- Advertise the **provider-prefixed** form (`openai/gpt-5.6-sol`) — it matches
  `capability_record.underlying` and the 0.5.2 "stable served-by-explicit names"
  convention, and is directly callable given P-4's twins.
- Omit the header entirely when there is no successor. Never guess (same rule as
  `Served-By`, which omits rather than guessing an unknown provider).

### Tests

- [x] Header present with correct k/v for `gpt-5`; absent when no successor is mapped.
- [x] Routing is byte-identical with the header on and off — proves advisory-only.
- [x] Respects the transparency config toggle.
- [x] Header values are header-safe (reuse `_header_safe`).
- [x] Fires for streaming and non-streaming alike (matching the `Mutations` rule).
- [x] Grammar matches `_mutation_token` byte-for-byte (`;` separator, no space) —
      assert against the existing serializer, not a hand-rolled expectation.
- [x] A `model_successors` key or value absent from `model_list` **fails**
      `test_config_consistency.py` rather than silently dropping the header.
- [x] Empty/absent map → header never emitted, no exception.
- [x] **`requested=` reflects the client's original model even when routing rewrote
      it.** Force a cost-tier swap or failover, assert the header still reports what
      was asked for — the regression the `data["model"]` shortcut would cause.

---

## P-6a — ✅ TUI parameter schema inspection

**Status:** raised in review; design §11 Q7.

`airlock/tui/param_schemas.py:73-85` carries a parallel per-provider `ProviderSchema`
for `"openai"` with **no `reasoning_effort` field**, and `MODEL_OVERRIDES` at `:162`
is empty. The P-2 fix operates on the proxy path and does **not** propagate here.

- [x] Inspect whether the TUI needs the 5.6 names, the `none`/`xhigh`/`max` effort
      levels, or a populated `MODEL_OVERRIDES` (2026-08-01).
- [x] **No code change required.** The Test screen reads `model_list` at runtime, so
      all configured 5.6 aliases already appear without a duplicate catalog. It does
      not expose `reasoning_effort`; adding one would create a second authoritative
      capability table without a user need. Keep `MODEL_OVERRIDES` empty unless the
      TUI gains a supported reasoning-effort control.

---

## P-6c — ✅ SHIPPED IN 0.5.6 — Warn-only measurement and reporting

**Status:** the effort window and cross-tier instrumentation established queryable
enterprise-JSONL/reporting contracts. Enforcement is now implemented; retain these
measurements for post-release monitoring rather than as a release gate. Design §13 was
corrected on 2026-08-02 after source inspection.

P-2 and P-2b both replace silent behaviour with rejections. The affected client
population **cannot be enumerated from the code** — only measured. P-2's effort
instrumentation shipped warn-only first. P-2b's non-mutating instrumentation is ready
for deployment; its observation window begins only after deployed queryability is
verified.

### Changes

For P-2, warn-only mode = today's behaviour exactly (same substitution, same
translation, same served model), plus:
1. **WARNING**-level structured log (never DEBUG), with a stable `event` discriminator.
2. The advisory response header the enforcing version would send.
3. A mutation-ledger entry, so it flows into `X-Airlock-Mutations` and the 0.5.4
   unified `RequestEvent` stream.

Required fields — this is the deliverable, not a side effect:

```
event=effort_would_reject       requested= translated_to= model=
                                supported= client_id=
```

- The effort event **must** reach the existing observability path — not log-file-only,
  so it is countable with the same tooling as everything else.
- `client_id` present wherever client identity is known, so impact attributes to
  specific callers rather than a bare total.

For P-2b, the separate measurement pack emits
`fuzzy_match_would_reject` with `requested`, `served`, `suggested`, `score`,
`from_tier`, `to_tier`, and `client_id`, preserves routing byte-for-byte, and adds a
ledger marker. Its own full billing-cycle window starts only after the deployed event
is queryable. Runbook:
[`runs/cross-tier-fuzzy-measurement-window.md`](runs/cross-tier-fuzzy-measurement-window.md).

### Tests

- [ ] Warn-only mode routes **identically** to current behaviour — assert byte-equal
      served model across the whole matrix.
- [ ] The effort event emits at WARNING with all required fields populated.
- [ ] The effort event is retrievable from the event store, not just the log file.
- [ ] `client_id` is populated when client identity is known.

### Historical measure-then-enforce sequence (design §13.3)

- [ ] **T-1** Warn-only release deployed; both events emitting.
- [ ] **T-2** Events verified queryable via existing tooling. If not, that blocks the
      measurement — it is not a docs gap.
- [ ] **T-3** Window agreed (recommend ≥2 weeks / ≥1 billing cycle) and observed.
- [ ] **T-4** Report: affected client count, request volume, distinct
      (requested → would-be-rejected) pairs, per event.
- [ ] **T-5** Per affected caller: notify / grace-extend / enforce.
- [x] **T-6** Enforce. `AIRLOCK_VALIDATE_REASONING_EFFORT=0` is a deliberate,
      documented short-term rollback switch, not the former default behavior.

---

## P-6b — MCP surface inspection

**Status:** raised in review; design §11 Q8.

- [x] Inspect `airlock/mcp_servers/` for anything enumerating models or model config
      (2026-08-01).
- [x] **No change required after inspection.** The MCP servers expose search/fetch
      tools and do not enumerate Airlock model aliases, read `model_list`, or provide
      a model-selection surface.

### Exit criteria

Clients on `gpt-5` are told in-band that `openai/gpt-5.6-sol` exists, with zero
routing change.

---

## P-7 — ✅ COMPLETE — Docs

**Files:** `docs/guide/routing.md:121-123`, model/provider guides, `CHANGELOG`.

- [x] Document the 5.6 family, the flat-price-tier framing, and the `>272K` surcharge.
- [x] Document that `gpt-5` deliberately stays on 5.5 and how to reach 5.6.
- [x] Document the `-pro` tier contract (may lag the flagship's version number).
- [x] Document current strict qualifier resolution, the 404 shape, and
      `X-Airlock-Model-Suggestion`.
- [x] Document `X-Airlock-Model-Alias`.
- [x] Update the tier table (it mirrors `_DEFAULT_COST_TIERS` in prose — a third
      copy that also drifts).
- [x] Document the current enforcement behavior and its rollback switch.

---

## P-8 — ✅ IMPLEMENTED — TUI configuration applicability ([#34])

**Goal:** let an operator know, before and after Apply, whether each changed setting
takes effect in the running proxy or needs a restart. The generic “Restart proxy for
full effect” message is not accurate enough for safe operation.

**Scope:** `airlock/tui/screens/config.py` and focused TUI tests only. This is a
configuration-screen change; it must not change proxy configuration loading,
request-path behavior, or `config.local.yaml` semantics.

### Design and acceptance criteria

- Maintain one explicit applicability map keyed by the ConfigPane field IDs. Each entry
  declares the environment variable and whether a proxy restart is required; do not
  infer this from a field label or a widget type.
- Add an unobtrusive visual marker and legend for restart-required controls and for
  controls that the current Apply action does not change.
- Capture the screen's applied baseline. On Apply, compare normalized current values
  against that baseline and partition changes into **live** and **restart required**.
  A click with no changes reports that truthfully.
- The post-Apply status names the changed restart-required controls. If every changed
  value is live, state that it is effective immediately; never promise that a
  restart-required setting has taken effect.
- The map must classify existing runtime behavior, not the desired behavior. At a
  minimum, `AIRLOCK_ENFORCE_MODE`, PII/keyword controls, and MCP tool allow/block lists
  are live; host/port, API keys, failover map, and logging destinations require restart.
  Resolve and document the status of every ConfigPane field before implementation;
  fields that the current Apply action does not persist must not be labelled effective.
- Tests cover the classification map, no-change detection, live-only status,
  restart-only status, and a mixed update. Preserve the existing redacted display of
  secrets and do not put secret values in the status message.

### Exit criteria

An operator can distinguish live changes from pending-restart changes without reading
source or restarting speculatively, and the screen accurately reports what its Apply
operation changed.

**Validation:** `uv run pytest tests/test_tui.py -q -k config` — 12 passed; scoped Ruff
check and formatting are clean (2026-08-02).

---

## P-9 — 🟡 PLANNED — Release hygiene and GitHub issue audit

**Goal:** make 0.5.8 release-ready with evidence that matches the shipped behavior,
not just passing local implementation tests.

### Scope and acceptance criteria

- Reconcile this plan, the changelog, release notes, and release evidence with the
  final enforcement and P-8 behavior. Historical measurement windows remain documented
  operational context; they must not appear as unclosed release gates.
- Run the normal release validation at the final commit: full test suite, lint, type
  checks, dependency audit, wheel build/clean-install check, Docker smoke test, and
  GitHub Actions. Record the results and any opt-in/live-test exclusions.
- Audit [GitHub issue #22](https://github.com/coreyt/airlock/issues/22) against the
  implementation and documentation: OpenAI-compatible chat-completions body, Gemini
  request controls, response headers, and structured log metadata. Close it only when
  the evidence shows that its decision-recording scope is fulfilled; otherwise add the
  precise missing work to a later release rather than expanding 0.5.8 implicitly.
- Prepare the version bump, changelog/release notes, release evidence, and normal
  release tag after the final CI commit. This is a published even-numbered release,
  unlike the internal 0.5.7 marker.

### Exit criteria

The release has a green, recorded validation set; its public notes disclose the
behavioral enforcement changes; and #22 is either closed with linked evidence or has a
specific, deferred follow-up.

---

## Sequencing

The enforcement work is complete. P-8 is the final code item, followed by P-9 release
hygiene. Post-release measurements continue to inform operations but do not hold the
release.

```
0.5.6  ✅ SHIPPED — catalog, 5.6 entries, tiers, fallbacks, litellm floor,
           dropped-qualifier guard, warn-only detection
0.5.7  ✅ INTERNAL CLOSEOUT — admission gate hardening and helpful refusal response
0.5.8  ✅ P-2/P-2b enforcement, P-6 disclosure, P-6a/P-6b inspection, P-7 docs
       ├── P-8  config applicability (#34) ✅
       └── P-9  release hygiene: #22 audit, version, changelog, build, CI, evidence/tag
```

## Deferred backlog

The open GitHub backlog is intentionally **not** an implicit 0.5.8 queue:

- The next operator-visibility release starts with the TUI catch-up plan's Pack A
  (mutation ledger plus served/attribution), then Pack B (provider headroom and spend).
  It is the natural home for issues such as #23–#28 and #31–#33.
- Logs search (#10, #29), virtual-key management (#30), batch live status, and dynamic
  semantic/tool-call/search architecture (#11, #16–#19) need separate scope and
  design.
- MCP startup timeout (#20) has an upstream LiteLLM component. Provider authorization
  (#21) is security-sensitive and overlaps the deliberately deferred Tavily work.
- #22 is handled explicitly by P-9; it is not a new product feature.

## Release checklist

- [x] 0.5.6 shipped (tagged v0.5.6, published to PyPI 2026-07-21).
- [x] Every callable alias appears in exactly one cost tier — forced-swap guard green.
- [x] P-2 and P-2b enforcement reviewed and implemented; post-release reporting stays
      enabled.
- [x] P-6a and P-6b resolved (implemented or explicitly recorded as no-change).
- [x] P-8 implemented and reviewed, including its focused TUI tests.
- [ ] P-9 complete: final validation recorded, #22 audited/closed-or-deferred, version
      and release notes prepared, and the normal 0.5.8 release tag created.
