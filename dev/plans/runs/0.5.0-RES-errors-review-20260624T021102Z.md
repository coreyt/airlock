# Pack 0.5.0-RES-errors review — codex gpt-5.5 (20260624T021102Z)

---

## Verdict: BLOCK

Findings:

- **BLOCK** [airlock/fast/guardian.py:106]( /home/coreyt/projects/airlock/airlock/fast/guardian.py:106), [airlock/fast/guardian.py:114]( /home/coreyt/projects/airlock/airlock/fast/guardian.py:114), [airlock/proxy_errors.py:51]( /home/coreyt/projects/airlock/airlock/proxy_errors.py:51), [airlock/proxy_errors.py:61]( /home/coreyt/projects/airlock/airlock/proxy_errors.py:61): `reason` is not actually sanitized. `_sanitize_reason()` only truncates `error.airlock.reason` to 300 chars, and `_raise_provider_protection()` embeds raw `reason` into the top-level exception message. `block_response_payload()` then emits that raw message as `error.message`, so upstream text can still leak secrets and bloat the response. This violates the design note’s “truncates and strips any key-like tokens defensively” requirement.

- **CONCERN** [airlock/proxy_errors.py:110]( /home/coreyt/projects/airlock/airlock/proxy_errors.py:110), [dev/notes/design-rate-limit-client-errors.md]( /home/coreyt/projects/airlock/dev/notes/design-rate-limit-client-errors.md): the handler is registered only for `AirlockProviderBlocked`, not base `RateLimitError`. That matches the current code comment and preserves LiteLLM’s passthrough provider-429 handling, but it diverges from the design note’s hard decision/edge case that plain provider `RateLimitError`s should also get OpenAI-shaped `provider_rate_limit` bodies and `Retry-After`. Decide whether this is an intentional scope reduction; otherwise provider 429 shaping is missing.

- **CONCERN** [tests/test_proxy_errors.py:79]( /home/coreyt/projects/airlock/tests/test_proxy_errors.py:79): I cannot confirm from these files that an `AirlockProviderBlocked` raised inside the LiteLLM pre-call hook bubbles to FastAPI’s exception handler rather than being caught/serialized by LiteLLM first. Current tests call the handler/helper directly, not through the proxy app request path. Needs an integration verification for the new seam.

Verified clean:

- Body is OpenAI-shaped and enriched under `error.airlock`; handler returns status `429`.
- `Retry-After` uses `ceil(cooldown)` with min `1`; `X-Airlock-Provider-State` and `X-Airlock-Block-Scope` are set.
- `AirlockProviderBlocked` subclasses `RateLimitError`, preserving `isinstance(exc, RateLimitError)`.
- Install path is idempotent via `app.state`, returns `False` without a FastAPI proxy app, and is called before `install_batch_gateway_on_proxy_app()`. Exception handler registration is order-independent, so no batch gateway conflict found.
