# 0.5.0-OBS-metrics-tui — codex review (promoted verdict)

**Reviewer:** codex `gpt-5.5` (high, read-only).
**Target:** branch `obs-metrics-tui`, HEAD `db7fae5` (impl `a7f484a`). Baseline `ae00f52`.
Merged `c31b362`. Raw log: `dev/plans/runs/0.5.0-OBS-metrics-tui-review-<ts>.log`.

## Verdict: PASS (no concern-level findings)

- **[info]** the metrics callback instantiates + `_self_register()`s at module import —
  same pattern as `enterprise_logger`/`monitor`; tests assert repeat registration leaves
  exactly one entry (idempotent). No action.

What codex confirmed:
- `classify_backend_kind` is a pure extraction (gateway: bedrock/azure/vertex_ai; native:
  anthropic/openai/gemini; else unknown); `attribute_served_backend` reuses it with no
  dataclass/signature change — existing served-attribution tests stay green.
- `airlock_mutations_total{field,op}` incremented once per ledger entry from the **success
  path only** (no failure-path double-count of the pre-call ledger); missing/empty/
  non-iterable/missing-field-or-op guarded; label cardinality bounded.
- TUI "Served via" column is observe-only (derived from the provider name; empty/fallback
  row has a matching cell). Scope clean (only metrics.py/transparency.py/overview.py).

## Orchestrator triage
**PASS → MERGED (`c31b362`).** Final transparency pack. All 7 packs CLOSED.
