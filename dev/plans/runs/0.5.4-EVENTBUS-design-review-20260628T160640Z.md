# 0.5.4 DESIGN gate #4 (TERMINAL) — codex review (promoted verdict)

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`
- **Target:** `dev/notes/design-request-event-bus.md` **rev 4** + anchored modules
- **Branch / base:** `feat/0.5.4-eventbus` @ base `57010d1`
- **Review log:** `dev/plans/runs/0.5.4-EVENTBUS-design-review-20260628T160640Z.log`
- **Gate history:** #1 BLOCK (4, 2 high) → #2 BLOCK (3, 1 high) → #3 BLOCK (2, 0 high)
  → **#4 PASS (0 findings).**

---

## Verdict: PASS

**Findings: none.**

## What passed on inspection (codex)
- Gate #3 fixes confirmed: §5a table + §5b pin metrics as sync+async × success+failure;
  `requests_total` increments on **both** paths (success="true" `metrics.py:189`,
  success="false" `metrics.py:216`), `request_duration` (`:191`) + `_record_mutations`
  (`:195`) success-only. Monitor table correct (sync+async × success+failure,
  `monitor.py:389-392` + both config lists `config.yaml:548-549`).
- Holistic: the `RequestEvent` superset includes raw `response_obj`, `request_headers`,
  `mcp_arguments`, `bare_exception_error`, guardrail enrichment, mutations,
  served/attribution; fathom raw-response + bare-vs-rich error hazards pinned; the seam
  is one fan-out callback with per-sink isolation, installed in enterprise's slot
  before monitor; out-of-scope boundaries explicit (precall-block, batch,
  admin_action, standalone guardrail metrics). **"No genuine design flaw remains."**
  Migration golden-test obligations judged "strong enough to catch the remaining
  implementation hazards."

---

## Orchestrator triage

**Call: PASS → DESIGN gate CLOSED.** DoD item 1 (design closed with codex PASS) is
met. The design note rev 4 is the authoritative `RequestEvent` + seam contract for
Phase E. No residual CONCERNs to carry. Proceed to Phase E starting with the **EVENT**
pack (canonical `RequestEvent` model + recorder/dispatcher seam), then the MIGRATE
ladder ([enterprise+fathom] → s3 → sql → sidechannels), VERIFY, DOCS.

The convergence record (4→3→2→0 findings) is retained across the four promoted
verdict files as the design's audit trail.
