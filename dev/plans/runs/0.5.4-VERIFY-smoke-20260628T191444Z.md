# 0.5.4 VERIFY — isolated-instance parity smoke (orchestrator-run)

- **When:** 2026-06-28 ~19:14Z · **Runner:** orchestrator (HITL-authorized agent-run)
- **Instance:** `dev/smoketest/run_isolated_instance.sh start` on **port 4137**, isolated
  `.runtime/` dir, copied config, remote sinks neutralized (S3/SQL/FathomDB off),
  `AIRLOCK_ENFORCE_MODE=observe`. Live `:4000`/`:8090` **never touched** (refused by
  construction). Probes via `/health/liveliness` only.

## Result: GREEN ✅

### 1. Clean boot + the ordering invariant — validated LIVE (the thing unit tests mock)
Launch log:
```
Initialized Success Callbacks - [RequestRecorderCallback, AirlockFastMonitor]
Initialized Failure Callbacks - [RequestRecorderCallback, AirlockFastMonitor]
```
- The **recorder is the SOLE telemetry callback** (no enterprise/fathom/s3/sql/metrics
  separately registered → no double-emit) and is registered **before `proxy_monitor`** on
  **both** success and failure lists — exactly the §5a/§5b ordering invariant (the recorder
  builds the event before monitor mutates `airlock_provider_protection` on the failure
  path). Proven in the real litellm proxy, not a mock.
- Recorder sinks in the smoke env: `['enterprise', 'metrics']` (s3/sql/fathom correctly
  gated off — their `AIRLOCK_ENABLE_*` flags unset). Clean boot, `/health/liveliness` 200.

### 2. Live transparency headers intact
Native alias `gemini-3.5-flash-aistudio` → `x-airlock-served-by: gemini` (real backend,
not the alias), `x-airlock-mutations: fallbacks=suppressed`, no error.

### 3. Live enterprise JSONL record — field-for-field complete (parity oracle)
The record produced by the **live recorder → `record_event` → `_write_log`** path carries
the full expected enterprise field set (35 keys): core (`timestamp`, `record_type:"request"`,
`success`, `model`, `user`, `team`, `request_id`, `messages`, `response`, `error`/`error_type`/
`failure_category`, `airlock_provider`, `start_time`/`end_time`, `duration_ms`, usage), the
**Gemini enrich-once** output (`airlock_gemini`, `airlock_gemini_response`,
`airlock_response_headers` — proving §3.5 enrich-before-snapshot ran in the live path), all
`guardrail_meta` `airlock_*` keys, and transparency (`mutations`, `served`, `attribution`,
`airlock_client`). Identical shape to the pre-migration `_build_record` output — the only
delta is the registered `timestamp` convergence. **No logged-field change.**

### 4. No dispatch/sink errors
The single launch-log `Traceback` was the orchestrator's own **unauthenticated `/metrics`
curl → 401** ("Malformed API Key... needs Bearer prefix") — not a recorder/sink error.
No `raised during dispatch` lines. Instance stopped cleanly; `.runtime/` is isolated.

## DoD coverage
Satisfies DoD item 6 (isolated-instance parity run green on a separate port, live `:4000`
untouched) and corroborates AC-EQUIV (live enterprise record unchanged) + AC-SEAM (single
ordered recorder, no double-emit) + AC-SMOKE (parity oracle: no served/logged field change).

> Harness note (not a 0.5.4 issue): the copied `.env`'s `AIRLOCK_LOG_DIR=./logs` won over
> the appended absolute override (dotenv/existing-env ordering), so the JSONL landed in
> `.runtime/logs/` rather than `.runtime/state/logs/` — still fully isolated under
> `.runtime/`, production untouched. Worth a one-line fix to `run_isolated_instance.sh`
> later (out of 0.5.4 scope).
