# 0.5.2 CAP-v1models — Code Review (codex gpt-5.5, reasoning=high)

**Diff:** `feat/0.5.2-CAP-v1models` @ 2250a62 vs base 88656b8 (inlined; unmerged local branch).
**Date:** 20260627T135029Z
**Outcome (round 1):** **CONCERN** — ASGI seam sound; one install-time robustness gap; fix in flight.

## Orchestrator triage
**Round 1: CONCERN.** Codex confirmed the load-bearing parts are correct: the ASGI
response-transform (GET /v1/models + /models only; full-body buffering incl. more_body;
transform only on 200+JSON+dict-with-list-data; byte-for-byte pass-through otherwise;
content-length recomputed + transfer-encoding dropped; additive-only — standard OpenAI
fields intact, unknown ids untouched), the dual pre/post-start install + idempotency flag,
the one-line safe wire-in, and the 14 tests (honest behavioral assertions). `capability_record`
reused, not reimplemented. No scope creep.

**The one CONCERN (fix in flight, same worktree):** `_build_capability_map` runs at
import/install time in the proxy process, so it must NEVER raise — but a truthy non-list
`model_list` (e.g. `model_list: true`) or a malformed entry could propagate an exception
and **break proxy startup**. Fix: validate `model_list` is a list; wrap per-entry
`capability_record(entry)` in try/except (skip+warn); skip non-dict / no-id entries — worst
case an empty/partial map ⇒ transparent pass-through. + RED-first degradation tests
(non-list model_list, one malformed + one good entry). Re-review the fix before merge.

## codex round-1 verdict (verbatim)
```
## Verdict: CONCERN

1. `airlock/models_seam.py:67` + `airlock/models_seam.py:73` - `_build_capability_map()` does not fully satisfy “invalid config degrades to empty/transparent.” A malformed truthy non-iterable `model_list` such as `model_list: true`, or an entry with malformed `litellm_params`, can raise during install and break proxy startup. Fix by validating `model_list` is a list and wrapping per-entry `capability_record(entry)` failures with skip/warn.

2. `tests/test_models_seam.py:220` - Add a real installer/config degradation test for malformed config, not just an empty injected map. Cover at least non-list `model_list` and malformed entry params so the explicit no-crash contract is locked.
tokens used
## Verdict: CONCERN

1. `airlock/models_seam.py:67` + `airlock/models_seam.py:73` - `_build_capability_map()` does not fully satisfy “invalid config degrades to empty/transparent.” A malformed truthy non-iterable `model_list` such as `model_list: true`, or an entry with malformed `litellm_params`, can raise during install and break proxy startup. Fix by validating `model_list` is a list and wrapping per-entry `capability_record(entry)` failures with skip/warn.
```
