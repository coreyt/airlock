# 0.5.3-LATENCY — Review (promoted verdict)

**Pack:** 0.5.3-LATENCY · **Branch:** `feat/0.5.3-LATENCY` · **Head:** `dbb305c`
**Reviewer:** `code-reviewer` subagent (sonnet) — **codex fallback** (codex 0.136.0
hit the bwrap user-namespace sandbox failure in this environment and emitted no
verdict; per `dev/agent-harness-reference.md` §3.2 the sonnet reviewer is the
sanctioned fallback). **Review date:** 2026-06-28.

## Verdict: PASS

Behavior-preserving, thread-safe, correctly ordered. Three informational nits, no
fixes required, no blockers.

## Risk areas verified

- **(a) Offload race-safety.** `mapping`/`counters` are created fresh per request;
  the MCP and messages `to_thread` calls are awaited **sequentially**, never
  concurrently — no shared-dict race. `_scrub_messages` builds new message dicts
  (`{**msg, "content": …}`), so reassigning `data["messages"]` after the await is
  clean; visibility is guaranteed by `Future` resolution.
- **(b) Post-redaction cache order.** `refresh_text_cache(data, call_type)` is
  called unconditionally **after** both awaits (`pii_guard.py:255`), reading the
  post-redaction `data["messages"]`. PII-disabled path early-returns before the
  refresh, so the first downstream caller (keyword) computes from unchanged
  messages. `TestTextCachePostRedaction` pins both paths.
- **(c) Cross-request isolation.** Cache under `data["metadata"]["_airlock_text"]`;
  new request = new `data` = fresh cache; `extract_text` guards on
  `isinstance(metadata, dict) and key in metadata`.
  `test_separate_requests_have_independent_caches` covers it.
- **(d) Prewarm failure-safety.** `prewarm()` swallows `except Exception`; lazy
  fetch unchanged so a cold cache still resolves. `test_prewarm_swallows_failure`
  exercises the handler.
- **(e) Scope.** `git diff main..HEAD --name-only` = exactly the 8 pack files; no
  locked (ACL/RACE/DECOUPLE) file, `config.yaml`, or `pyproject/uv.lock` touched.
- **Concurrency test validity.** `asyncio_mode = "auto"` collects the bare
  `async def` tests; `clean_env` autouse + `_env_flag` default-True enables PII so
  the `to_thread` path runs; `_SlowAnalyzer.analyze` matches the call signature.

## Findings (all [nit], non-blocking)

1. `local_vllm_router.py:140` — `# pragma: no cover` on the `prewarm()` except
   block is contradicted by `test_prewarm_swallows_failure` (harmless/misleading).
2. `tests/test_local_vllm_router.py:220` — `test_prewarm_populates_cache` asserts
   the mock was awaited but not that `_loaded_cache` populated (name oversells).
3. `dev/notes/0.5.3-LATENCY-design.md` — references a module-level
   `prewarm_local_vllm_router()` helper not shipped; memo states this is
   intentional (startup wiring deferred to avoid touching DO-NOT-TOUCH
   `proxy_bootstrap.py`). DoD only requires the failure-safe method, which exists.

## Orchestrator triage

PASS accepted; the three nits are informational and do **not** warrant a fix pack
(no correctness/scope impact). Proceed to merge `feat/0.5.3-LATENCY` → `main` after
the full no-network suite confirms green. Nits logged as optional future cleanup.
