# 0.5.4-MIGRATE-entfathom-wire (2b-i) — codex pack review (promoted verdict)

- **Reviewer:** codex `gpt-5.5`, high effort, read-only
- **Target:** branch `worktree-agent-a0636cf47c03e53d0`, commits `398b8d3..2b50ad4`
- **Baseline:** `398b8d3`
- **Review log:** `dev/plans/runs/0.5.4-MIGRATE-entfathom-wire-review-20260628T170312Z.log`

## Verdict: PASS — findings: none.

**What passed (codex):**
- **Dormancy holds:** `recorder.py` constructs the recorder + callback but calls NO
  LiteLLM registration API; `callbacks/__init__.py` is empty so the module isn't pulled
  in by package import. No existing callback/registration/builder altered.
- `RequestRecorder` backward-compatible: `async_only=False` default; `dispatch(event)`
  defaults `is_async=True`; lock snapshot + per-sink isolation retained.
- `RequestRecorderCallback`: sync→is_async=False, async→`to_thread(partial(...))`+
  is_async=True, success flags correct; building inside to_thread preserves Gemini
  in-place enrichment semantics.
- enterprise/fathom `record_event` faithful: enterprise writes exactly
  `project_enterprise(event)` + correct log formats; fathom preserves skip/engine
  guard/call-id fallback/dedup/write via `project_fathom(event)`.
- Tests cover async-only skip, sync-vs-async dispatch, enterprise projection fidelity,
  fathom skip/no-engine/dedup/write, and dormancy.

## Orchestrator triage
**PASS → merge.** No findings; behavior-preservation (dormancy) verified. Merge 2b-i
into `feat/0.5.4-eventbus`; then author 2b-ii cutover (activate recorder in
enterprise's slot before `proxy_monitor`; remove old registrations; delete
`_build_record`/`_base_record`/`_fathom_properties` + old callback methods).
