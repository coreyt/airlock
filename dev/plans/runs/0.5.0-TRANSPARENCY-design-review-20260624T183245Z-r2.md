# 0.5.0 Transparency — design-time codex review (promoted verdict)

**Reviewer:** codex `gpt-5.5` (`-c model_reasoning_effort=high --sandbox read-only`)
**Target:** the 0.5.0 transparency workstream design set (pre-implementation), at
branch `feat/0.5.0-resilience-admin`.
**Reviewed files:** `dev/notes/design-mutation-and-provider-transparency.md`,
`dev/architecture.md` §3.7, `dev/user-needs.md` UN-19/UN-20,
`dev/plans/0.5.0-transparency-plan.md`.
**LiteLLM target:** 1.89.0 (claims verified against `.venv`).
**Logs:** round 1 `0.5.0-TRANSPARENCY-design-review-20260624T182224Z.log`;
round 2 `0.5.0-TRANSPARENCY-design-review-20260624T183245Z-r2.log`.

---

## Verdict: PASS (round 2)

Round 1 returned **BLOCK** with 7 findings (2 high, 4 medium, 1 low). All were
real design holes. Each was resolved by editing the design/plan/architecture/
user-needs; round 2 re-verified each as **RESOLVED** with no new high/medium issue.

| # | Sev | Finding (round 1) | Resolution | Status (round 2) |
|---|-----|-------------------|------------|------------------|
| 1 | high | streaming served-by read `_hidden_params["custom_llm_provider"]` — absent at header-flush time | read wrapper attr `response.custom_llm_provider` (set `streaming_handler.py:113`) with `_hidden_params` fallback; omit header when unknown (Decision 3, §4.1, CC-T3) | RESOLVED |
| 2 | high | `drop_params` had no implementation path | Decision 8 — derive via `detect_dropped_params()` + `litellm.get_supported_openai_params()`; OBS-ledger owns it | RESOLVED |
| 3 | med | served accounting undefined on failure/429 (no response) | Decision 4 / CC-T4 — success keys off served; failure keys off error provider else inferred; final-response attribution | RESOLVED |
| 4 | med | cross-callback ordering could drop served/cost | Decision 5 — logger calls `attribute_served_backend()` itself with `kwargs["response_cost"]` fallback | RESOLVED |
| 5 | med | generic `field=after` header could leak system-prompt/message content; redaction value-free only by convention | CC-T2 — `record_redaction()` + ctor rejects value for `op:redact`; header `HEADER_VALUE_FIELDS` allowlist | RESOLVED |
| 6 | med | `X-Airlock-Explain` body envelope not SSE-safe | Decision 7 — envelope is non-streaming only | RESOLVED |
| 7 | low | "byte-for-byte unchanged" overstated (headers change) | tightened: body unchanged by default; additive default-on headers/log fields with opt-outs (CC-T7) | RESOLVED |

## Orchestrator triage

**KEEP all resolutions; gate PASSED.** The design is implementation-ready. No
overrides. Phase E may begin (first pack `0.5.0-OBS-core`), driven from
`dev/plans/prompts/0.5.0-transparency-ORCHESTRATOR.md` in a dedicated session.

### Carry-forward notes for implementers (from the review)
- `attribute_served_backend` must be tolerant: `response_cost is None` at the header
  hook, complete at the success hook; provider via `_hidden_params` **or** the
  wrapper attribute.
- `Mutation.__post_init__` enforcing the redact invariant is a RED test in OBS-core.
- OBS-accounting must not assume a served read exists on the failure path.
