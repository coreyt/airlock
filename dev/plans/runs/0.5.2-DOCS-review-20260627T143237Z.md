# 0.5.2 DOCS — Docs Review (codex gpt-5.5, reasoning=high)

**Diff:** `feat/0.5.2-DOCS` vs base 4610db0. **Date:** 20260627T143237Z
**Outcome:** **CONCERN → fixed → PASS.** mkdocs `--strict` clean throughout.

## Orchestrator triage
**PASS after one fix.** Codex confirmed accuracy on the load-bearing points: the
anti-overclaim line (Vertex chat-only at `global`), the alias map vs config, the
discover→pin→verify recipe + header names, UN-21/UN-22 acceptance, and the changelog.

**One CONCERN (fixed, `dc4b100`):** `batch.md` listed OpenAI under "batch-capable
stable aliases (`batch ∈ endpoints`)", but shipped OpenAI entries have no `airlock_batch`
marker → `endpoints: ["chat"]`. OpenAI native batch works via LiteLLM passthrough
(`custom_llm_provider=openai` + upstream id) but is **not advertised** in `airlock.endpoints`.
Removed OpenAI from that table; added a clarifying note. The `endpoints` field now never
over-claims (orchestrator-applied; mkdocs `--strict` re-verified clean).

## codex verdict (verbatim)
```
## Verdict: CONCERN

1. `docs/guide/batch.md:56` — The table says OpenAI models are shipped batch-capable stable aliases with `"batch" ∈ endpoints`, but `endpoints_for()` only adds `batch` for `airlock_batch` or regional Vertex entries. Shipped OpenAI config entries do not advertise `batch`. Fix by removing OpenAI from this `endpoints` table, or clearly moving it to a separate note: OpenAI native batch works via `custom_llm_provider=openai`/upstream IDs but is not currently advertised in `airlock.endpoints`.
tokens used
## Verdict: CONCERN
```
