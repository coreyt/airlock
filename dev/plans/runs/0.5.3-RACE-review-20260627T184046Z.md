# 0.5.3-RACE — codex review (promoted)

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`,
  `--cd` the RACE worktree.
- **Target:** branch `feat/0.5.3-RACE`, impl HEAD `b458c7d` (closure `28432d1`).
  Baseline `6aeea94`.
- **Review log:** `dev/plans/runs/0.5.3-RACE-review-20260627T184046Z.log`
- **Note:** codex's sandbox could not run `git diff`/`grep`/tests (`bwrap: loopback`
  failure); the verdict is from direct file inspection.

---

## Verdict: CONCERN → **RESOLVED**

The core fix passed inspection: `StateStore.record_threat_score` holds
`StateStore._lock` across the full RMW (request-time snapshot, prior `threat_score`
read, decay, max-blend, `threat_score` write, and `backoff_until` write);
`assess_threat` keeps heuristics + logging lock-free; no nested lock acquisition;
constants/decay-basis/threshold/backoff formula preserved with single-thread
characterization coverage. Identity routing is behavior-preserving; scope clean (no
DO NOT TOUCH file edited). Three test/edge-case findings:

### 1. [medium] no-lost-update test not deterministic against the old race
Refs: `tests/test_threat_score_race.py`, `tests/test_state_stress.py`
The stress test would fail pre-fix mainly because `record_threat_score` didn't
exist, not because the old outside-lock RMW is deterministically exposed.

### 2. [low] request extractor misses the documented `None` edge case
Refs: `airlock/client_identity.py:extract_airlock_client_from_request`
`(None, ...)` called `.get` on `None` and raised, contradicting the closure's
`None -> no_client` claim.

### 3. [low] golden identity parity thinner than the acceptance bar
Refs: `tests/test_client_identity.py`
enterprise_logger parity checked only two cases; representative empty/whitespace/
missing-header set not asserted against the legacy paths.

---

## Orchestrator triage — CONCERN resolved (FIX applied, not overridden)

CONCERN, no BLOCK. The core race fix is sound; I strengthened the tests + closed the
edge case directly in the worktree (commit `e0b6596`) rather than overriding, so
AC-RACE is met by a real no-lost-update assertion:

- **F2 (real gap) → FIXED.** `extract_airlock_client_from_request` now guards
  `data = data or {}`; signature widened to `Mapping | None`; docstring states the
  `None -> api-key/no_client` contract. Added `(None, None)` and
  `(None, key)` golden cases.
- **F1 → FIXED with a deterministic probe.** Added
  `test_record_threat_score_rmw_is_serialized`: a duck-typed client whose
  `threat_score` getter widens the read→write window (sleep) and tracks concurrency
  depth via a shared counter; two `Barrier`-synchronized threads call the mutator.
  Under the lock the observed depth never exceeds 1; **an unlocked RMW would
  deterministically observe depth 2** (the injected window guarantees overlap), so
  the test genuinely catches a regression to the old behavior — not scheduling luck.
  (Complements the existing `_CountingLock` single-acquisition test.)
- **F3 → FIXED.** Expanded `test_enterprise_logger_parity` to the representative
  input set (present / whitespace-only / missing / legacy `client_id` fallback) and
  asserted parity against the canonical `normalize_client_id`; added empty-mapping /
  missing-header golden rows.

**Verification:** `uv run --extra test python -m pytest tests/test_threat_score_race.py
tests/test_client_identity.py tests/test_fast_threat_detector.py` → 65 passed;
ruff clean. Post-merge on main: 241 passed across RACE+ACL targeted sets.

**Config-loading consolidation** was intentionally deferred per the pack's §4
("ship the race fix alone if consolidation balloons") — recorded in `output.json`;
it can be revisited in a later cleanup.

**Gate: cleared → merged** (`fe45159`).
