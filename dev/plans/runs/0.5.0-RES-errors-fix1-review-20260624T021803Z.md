# RES-errors fix-1 re-review — codex (20260624T021803Z)

---

## Verdict: PASS

No blocking findings.

Verified:
- `sanitize_reason()` redacts `sk-*`, `Bearer *`, and 32+ char key-like tokens, then truncates. The regex uses simple alternation and bounded character classes, with no nested quantifiers or backtracking-heavy shape: [airlock/proxy_errors.py](<repo-root>/airlock/proxy_errors.py:23).
- `_raise_provider_protection()` sanitizes `reason` before embedding it into `message` or the raised `AirlockProviderBlocked`: [guardian.py](<repo-root>/airlock/fast/guardian.py:116).
- `block_response_payload()` defensively sanitizes both top-level `error.message` and nested `error.airlock.reason`: [proxy_errors.py](<repo-root>/airlock/proxy_errors.py:78), [proxy_errors.py](<repo-root>/airlock/proxy_errors.py:87).
- Client headers do not include upstream reason text; they only include `Retry-After`, provider state, and block scope: [proxy_errors.py](<repo-root>/airlock/proxy_errors.py:94).

Previously clean items still hold from these files: OpenAI-shaped error body, HTTP 429 handler, `Retry-After = ceil(cooldown)` with min 1, `X-Airlock-*` headers, `AirlockProviderBlocked` subclassing `RateLimitError`, handler `isinstance` assertion, and idempotent install guard.

Note: guardian still records raw `last_reason` into internal metadata/logger args before raising in two quarantine branches, but I did not find any path in the requested files where that raw value reaches the client body or headers.
