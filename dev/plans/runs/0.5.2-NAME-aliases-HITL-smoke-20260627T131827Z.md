# 0.5.2 NAME-aliases — post-merge HITL live smoke (isolated instance)

**When:** 2026-06-27 · **Who:** orchestrating agent (K2 default) · **How:**
`dev/smoketest/run_isolated_instance.sh` on **PORT=4137**, copied config + .env,
remote sinks neutralized, `/health/liveliness` only. Live `:4000` (litellm pid
457947) and `:8090` **never touched** ([[airlock-production-safety]]). Operator
confirmed Vertex creds present; the isolated venv needed the `vertex` extra
(`google-auth`) installed for the vertex probe (then restored dev tools).

**Gate (orchestrator):** model ids are a client contract — pin a call on a NEW
`aistudio/`, `vertex/`, `anthropic/`, `openai/` alias; confirm `X-Airlock-Served-By`
reports the right provider and **no `X-Airlock-Model-Override`** appears.

## Result — PASS (alias contract proven)

| Probe (pinned) | HTTP | X-Airlock-Served-By | Model-Override | Verdict |
|---|---|---|---|---|
| `aistudio/gemini-3.5-flash` | 200 | **`gemini`** | absent | ✅ consolidated AI-Studio entry serves + attributes correctly |
| `vertex/gemini-3.5-flash` | 200 | **`vertex_ai`** | absent | ✅ routes to a **genuinely different** backend than aistudio/ — the headline disambiguation |
| `anthropic/claude-haiku` | 200 | absent¹ | absent | ✅ pinned; identical to bare `claude-haiku` (transparent) |
| `openai/gpt-5-nano` | 429² | — | absent | ⧗ routed to OpenAI correctly; upstream quota (env), not an alias defect |

All probes carried `X-Airlock-Mutations: fallbacks=suppressed` ⇒ **auto-pin works for
slash aliases** (429-not-swap preserved); **no `X-Airlock-Model-Override` on any probe**.

**The headline proof:** `aistudio/gemini-3.5-flash` → `gemini` and
`vertex/gemini-3.5-flash` → `vertex_ai` resolve to **different providers with correct,
header-verifiable attribution** — exactly the AI-Studio-vs-Vertex confusion (the
motivating 2026-06-24 thread) that this release exists to kill.

### Notes / non-blocking

1. **¹ anthropic served-by absent is PRE-EXISTING, not a NAME-aliases regression.**
   Bare `claude-haiku` shows the same absence (side-by-side comparison) — so the
   slash alias is **transparent** to served-by attribution (the design's core claim).
   The anthropic `custom_llm_provider`→header surfacing gap is independent of this
   pack (it predates 0.5.2); track as a transparency follow-up, not a 0.5.2 blocker.
   For comparison, `gemini-3.5-flash` (bare) and `aistudio/gemini-3.5-flash` both
   report `gemini` — bare/slash identical.
2. **² openai 429 = upstream quota** (`OpenAIException - You exceeded your current
   quota`), an account/billing/env condition; routing to OpenAI was correct and the
   pin held (no fallback swap). Re-verify at release sign-off when quota allows.

**Production safety:** ✅ separate port + copied config; `:4000`/`:8090` untouched;
`/health/liveliness` only.
