# 0.5.0-OBS-log — codex review (promoted verdict)

**Reviewer:** codex `gpt-5.5` (high effort, read-only), at zero concurrent load.
**Target:** branch `obs-log`, HEAD `bed3663` (impl `a9f9c8f`). Baseline `d1ca77f`.
**Raw log:** `dev/plans/runs/0.5.0-OBS-log-review-<ts>.log`.

## Verdict: PASS (no findings)

What codex confirmed:
- **Decision 5** — `_build_record` calls `attribute_served_backend(response_obj,
  cost_fallback=kwargs.get("response_cost"))` itself; no header-hook dependency.
  `airlock_provider` (inferred) preserved; `attribution="served"` only when the served
  provider resolves.
- **CC-T2 value-safety** — `mutations` is the full ledger via `dataclasses.asdict`;
  redact records are value-free, inject/rewrite carry `after=None`; no secret/prompt
  content can appear.
- **try/except guard acceptable** — wraps only the attribution call, logs at debug with
  `exc_info`, does not cover ledger serialization/`_write_log`; fallback to
  `served=None`/`inferred` is appropriate for a logging callback.
- **No schema break** — `record_type` still `"request"`; existing fields unchanged; the 3
  new fields purely additive. None/empty cases covered. 68 tests green.

## Orchestrator triage
**PASS → MERGE as-is.** (Note: OBS-log's guard already logs `exc_info`, which is exactly
the observability OBS-accounting's guard was missing — hence the accounting fix.)
