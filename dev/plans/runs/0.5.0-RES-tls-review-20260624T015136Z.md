# Pack 0.5.0-RES-tls review — codex gpt-5.5 (20260624T015136Z)

---

## Verdict: PASS

No blocking findings.

Checked:

- `_ssl_cli_args()` strips both env vars and only returns TLS args when both are non-blank: [airlock/proxy.py](/home/coreyt/projects/airlock/airlock/proxy.py:108).
- Partial config warns and returns plain HTTP flags: [airlock/proxy.py](/home/coreyt/projects/airlock/airlock/proxy.py:112).
- Default/plain HTTP path remains unchanged because no SSL flags are appended unless `_ssl_cli_args()` returns values: [airlock/proxy.py](/home/coreyt/projects/airlock/airlock/proxy.py:397).
- Wiring is correctly placed after `--host/--port` and before `subprocess.run`: [airlock/proxy.py](/home/coreyt/projects/airlock/airlock/proxy.py:408).
- Flag names are accurate for the locally locked/installed LiteLLM `1.89.0`: `--ssl_keyfile_path` and `--ssl_certfile_path` are Click options in [proxy_cli.py](/home/coreyt/projects/airlock/.venv/lib/python3.12/site-packages/litellm/proxy/proxy_cli.py:729), and LiteLLM maps them to uvicorn `ssl_keyfile` / `ssl_certfile` at [proxy_cli.py](/home/coreyt/projects/airlock/.venv/lib/python3.12/site-packages/litellm/proxy/proxy_cli.py:1230).
- Security: blank/whitespace values do not slip through due to `.strip()`. `subprocess.run` uses an argv list, so there is no shell injection surface here.
- Tests cover both-set, neither-set, cert-only partial warning, blank values, main wiring with SSL, and default no-SSL flags: [tests/test_proxy.py](/home/coreyt/projects/airlock/tests/test_proxy.py:670).

Minor note: tests do not separately cover “key-only partial” or “one set, other whitespace,” but the implementation is symmetric after stripping, so I do not consider that a concern.
