# 0.5.2 COMPAT-tests — Code Review (codex gpt-5.5, reasoning=high)

**Diff:** `feat/0.5.2-COMPAT-tests` @ f8eb4cb vs base dd58bda (tests-only; inlined).
**Date:** 20260627T140849Z
**Outcome (round 1):** **CONCERN** — suite covers all 7 areas but several assertions were
loose/circular; hardening fix in flight.

## Orchestrator triage
**Round 1: CONCERN (5 test-rigor findings, no product bug).** A regression-lock that can't
fail is worthless, so all 5 are being fixed (not overridden):
1. **Area 5 circular** — used `capability_record` as oracle for both surfaces it builds →
   add hard-coded EXPECTED capability dicts; assert four-way equality (injected == seam-map
   == capability_record == EXPECTED).
2. **Injected model_info "exactly" was key-subset** → assert exact dict equality.
3. **Area 4 used `"batch" in endpoints`** → assert the FULL list (`["chat"]`/`["chat","batch"]`).
4. **Area 1 parity didn't prove both aliases RESOLVE to the same underlying** → assert both
   `alias_table.resolve()` non-None + same underlying provider-model.
5. **`wired` fixture leaked global singleton state** → yield + teardown reset.
Verified tests-only (zero airlock/ or config.yaml changes). Re-review the hardening before merge.

## codex round-1 verdict (verbatim)
```
## Verdict: CONCERN

1. `tests/test_0_5_2_capability_compat.py:274` + `:286` - Area 5 uses `capability_record(...)` as the oracle for both product surfaces, but those surfaces are also built from that helper. If `capability_record` drops `deprecated`, changes `underlying`, or returns a wrong `airlock_provider`, these tests can still pass. Fix: add hard-coded expected capability dicts for representatives and assert `injected_model_info[name] == cap_map[name] == capability_record(entry) == EXPECTED[name]`.

2. `tests/test_0_5_2_capability_compat.py:278` - The injected `model_info` test only checks expected keys, despite claiming “exactly”. Extra stale keys in `model_info` would pass. Fix: replace the loop with exact equality, or explicitly project and assert the exact Airlock capability key set.

3. `tests/test_0_5_2_capability_compat.py:203` - Area 4 only checks `"batch" in endpoints_for(entry)`, so unsupported extra endpoints would pass for many entries. Fix: assert the full endpoint list for every entry: `["chat", "batch"]` when `_expects_batch(entry)` else `["chat"]`.

4. `tests/test_0_5_2_capability_compat.py:96` + `:103` - Area 1 does not prove both old and new aliases resolve through `alias_table`, only provider inference plus legacy non-None resolution. Fix: for each old/new pair, assert both resolve and their resolved entries have the same underlying provider model.

5. `tests/test_0_5_2_capability_compat.py:62` - `wired` reloads `alias_table` but never restores it. `_alias_provider_map` is reset by conftest, but `alias_table._loaded` can leak across later tests. Fix: make `wired` a `yield` fixture and reset/reload the alias table state on teardown, or add a shared conftest reset for the alias singleton.
tokens used
## Verdict: CONCERN

1. `tests/test_0_5_2_capability_compat.py:274` + `:286` - Area 5 uses `capability_record(...)` as the oracle for both product surfaces, but those surfaces are also built from that helper. If `capability_record` drops `deprecated`, changes `underlying`, or returns a wrong `airlock_provider`, these tests can still pass. Fix: add hard-coded expected capability dicts for representatives and assert `injected_model_info[name] == cap_map[name] == capability_record(entry) == EXPECTED[name]`.

2. `tests/test_0_5_2_capability_compat.py:278` - The injected `model_info` test only checks expected keys, despite claiming “exactly”. Extra stale keys in `model_info` would pass. Fix: replace the loop with exact equality, or explicitly project and assert the exact Airlock capability key set.

3. `tests/test_0_5_2_capability_compat.py:203` - Area 4 only checks `"batch" in endpoints_for(entry)`, so unsupported extra endpoints would pass for many entries. Fix: assert the full endpoint list for every entry: `["chat", "batch"]` when `_expects_batch(entry)` else `["chat"]`.

4. `tests/test_0_5_2_capability_compat.py:96` + `:103` - Area 1 does not prove both old and new aliases resolve through `alias_table`, only provider inference plus legacy non-None resolution. Fix: for each old/new pair, assert both resolve and their resolved entries have the same underlying provider model.
```

## Round 2 — CONCERN cleared (orchestrator-verified)
Fix `9b41c2a`: all 5 hardenings applied exactly. The new `_EXPECTED_CAP` is hard-coded
LITERALS (independent of `capability_record`), so the four-way oracle
(`injected == seam-map == capability_record == literal`) is non-circular — inversion-RED
confirmed it now catches a `capability_record` regression the old check missed. Area-4 asserts
full endpoint lists; Area-1 proves both old+new aliases resolve to the same underlying;
`wired` is now a yield fixture restoring both singletons (neighbor run 309 green, no leak).
54 tests, tests-only. **Verdict: PASS (after fix).** Merged.
