# Codex review — Pack 0.5.1-SET-warnratio

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`
- **Target:** branch `feat/0.5.1-SET-warnratio`, HEAD `5605313`
- **Baseline:** `f2706e9`
- **Review log:** `dev/plans/runs/0.5.1-SET-warnratio-review-20260626T224626Z.log`
- **Date:** 2026-06-26

## Verdict: CONCERN

### 1. [low] Closure artifact records stale head
Refs: `dev/plans/runs/0.5.1-SET-warnratio-output.json:5,6`

Branch HEAD is `5605313`, but `output.json` records `head_sha` as `a410abd` (the last code
commit) and its commit list stops before the closure commit. Metadata-only; does not affect
the implementation.

## What passed on inspection
Warn-ratio unification correct: router and monitor both read `get_settings().budget_warn_ratio`
(router for current + alternate provider comparison; monitor for near-limit warn). The
zero/absent-budget short-circuits (AC-0) are preserved. The conftest autouse reset is safe —
`set_router_config(None)` empties the catalog cleanly, runs around each test, and tests needing
a catalog can still configure one in their own setup. No scope creep in `state.py`/`settings.py`/
`config.yaml`. CHANGELOG accurately documents behavior-change #2.

## Orchestrator triage — OVERRIDE the sole LOW (with rationale)

The only finding is a LOW, metadata-only `head_sha` lag inherent to the "write output.json then
commit it" flow: the artifact correctly records the last *code* commit (`a410abd`); the only
later commit (`5605313`) is the chore that records the artifact itself — no functional content.
Per the merge gate, a LOW CONCERN may be **overridden with rationale** (no BLOCK is overridden).
**Decision: OVERRIDE — merge.** The pack's substance (warn-ratio unification, AC-0 preservation,
safe isolation fixture, CHANGELOG) is clean. No fix cycle warranted for cosmetic metadata.
(The merge commit and STATUS board record the true final HEAD, so promotion metadata is not lost.)
