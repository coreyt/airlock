# Pack 0.4.0-B Review — codex gpt-5.5 verdict

Reviewer: `codex exec --model gpt-5.5 -c model_reasoning_effort=high --sandbox read-only`.
Target: branch `0.4.0-B` HEAD `20fec37` (RED `aebac91` → GREEN `20fec37`).
Baseline: `5a1b564` (main with Pack A).
Review log: `dev/plans/runs/0.4.0-B-review-20260615T121038Z.log`.

## Verdict: CONCERN

### 1. [low] Batch label behavior implemented but not pinned by an assertion
Refs: `airlock/tui/screens/logs.py:245`, `tests/test_tui.py:338`
`_populate_table()` correctly labels batch rows `BATCH`, but the TUI test calls
`_populate_table()` without asserting the rendered cell, so the distinct-label DoD
item is implemented-but-under-tested. Implementation verified correct.

## What passed on inspection
`write_batch_record` routes through `_write_log` so redaction (`_redact_record`) +
rotation apply; record uses `airlock_provider`/`call_type="batch"`/`is_batch_call=True`
consistently with the TUI. Monitor batch exclusion skips model success/failure
(latency/health) stats while preserving client-level accounting. TUI filtering is
null-safe on `model=None`; Batch filter works; LLM view excludes batch.

## Orchestrator triage
**Orchestrator override 2026-06-15: CONCERN accepted.** Single low,
test-coverage-only finding; the implementation is correct (independently gated +
codex-confirmed) and the gap is prompt-induced (prompt said "extend null-safety
tests", not "assert the label text"). Per orchestration §7, accepting a
prompt-induced low CONCERN without a fix-N pass. **Follow-up (small):** add an
assertion that a batch-tagged record renders the `BATCH` label in
`_populate_table` — tracked, non-blocking. Merged to main.
