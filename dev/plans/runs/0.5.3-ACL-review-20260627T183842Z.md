# 0.5.3-ACL — codex review (promoted)

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`,
  `--cd` the ACL worktree.
- **Target:** branch `feat/0.5.3-ACL`, code HEAD `6d1a449` (closure `4eec946`).
  Baseline `6aeea94`.
- **Review log:** `dev/plans/runs/0.5.3-ACL-review-20260627T183842Z.log`
- **Note:** codex's sandbox could not run `git diff`/`grep`/tests (`bwrap: loopback`
  failure); the verdict is from direct file inspection via `sed`.

---

## Verdict: BLOCK → **CLEARED** (single finding fixed + verified)

### 1. [high] `additional_headers` read still outside the ACL
Refs: `airlock/fast/monitor.py:197-199`, `airlock/litellm_adapter.py:69`

`fast/monitor.py` migrated the `_hidden_params` read to `hidden_params(response_obj)`
but then still did `hidden.get("additional_headers")` — the nested-key read that
inventory §D marked `Migrate? = yes`, while the adapter already exposed
`additional_headers(response)`. Behavior-preserving, but it violated the pack's
"single owner of every LiteLLM-internal read" contract.

**What passed on inspection:** parity paths in `transparency.py` preserve provider
fallback, `vertex_ai_beta` normalization, unknown-provider header omission, and cost
fallback (Q1). The middleware-install mechanism is centralized in
`install_asgi_middleware()` and reused by `models_seam`/`batch`/`admin` without
changing install order. The 9 named §3.7 parity fixtures exist with literal expected
values. No DO NOT TOUCH module edited.

---

## Orchestrator triage — BLOCK cleared (FIX applied)

The BLOCK was the **sole** finding and a narrow completeness gap. I applied the
minimal behavior-identical fix directly in the worktree (orchestrator fix, not the
main checkout):

- `airlock/fast/monitor.py:199`: `hidden.get("additional_headers")` →
  `additional_headers(response_obj)`; import added on line 28. The
  `isinstance(hidden, dict)` gate is **kept** — so the "hp is a dict but the
  `additional_headers` key is absent" case still parses `{}` exactly as before (the
  adapter accessor conflates not-a-dict with key-absent, so removing the gate would
  have changed behavior). Commit `e41312b`.

**Verification (the reviewer's own prescribed checks, which its sandbox couldn't run):**
- `grep -rn "_hidden_params\|sys.modules.get(\"litellm.proxy.proxy_server\")\|app.middleware_stack" airlock/ --include=*.py | grep -v test`
  → only `airlock/litellm_adapter.py` (plus a code comment + a docstring). No direct
  nested-key reads remain outside the adapter.
- `uv run --extra test python -m pytest tests/test_fast_monitor.py tests/test_litellm_adapter.py` → **71 passed**.
- `ruff check` + `ruff format --check` on `monitor.py` → clean.

Because the fix is mechanical, directly closes the sole finding, and is verified by
the exact grep + targeted-test checks the reviewer specified, the BLOCK condition no
longer holds. **Gate: cleared → merge** at worktree HEAD `e41312b`.
