# 0.5.0 transparency — follow-up fix reviews (promoted verdicts)

Two follow-up fixes, both codex `gpt-5.5` (high, read-only), at zero load.

## OBS-served-streamfix — Verdict: PASS (merged `84f18c6`)
HITL smoke test found streaming served-by reported `vertex_ai_beta` for a native AI-Studio
gemini call. Root cause (litellm 1.89): the gemini stream wrapper hardcodes
`custom_llm_provider="vertex_ai_beta"` for BOTH AI Studio and Vertex; `api_base` differs
(generativelanguage.googleapis.com vs `{loc}-aiplatform.googleapis.com`). Fix:
`_normalize_served_provider` maps `vertex_ai_beta`→`vertex_ai` and resolves to `gemini` on the
AI-Studio host, so streaming + non-streaming converge. codex confirmed: exact-host compare
(no substring/None/port false-positives), non-Google providers can't be rewritten,
classification uses the normalized provider. 20 served + 120 transparency tests green. No findings.

## OBS-ledger-passthrough (Site 12) — Verdict: PASS (merged `48a2acc`)
Closes the OBS-ledger gap: the enhanced-passthrough **config-fallback** system-prompt injection
(Site 11 only covered the `litellm_params` path) now records one value-free `inject` via the
interceptor's `if not enhanced_profile:` branch, reusing the provider's own `_resolve_profile`.
codex confirmed: mutually exclusive with Site 11 (no double-record; non-enhanced records
nothing), CC-T2 value-free, CC-T4 observe-only (no behavior change), no circular import,
correct outer-metadata reachability, guards for missing model/target/system_prompt. 19 tests
green. No blocking findings.

## Net
UN-19 ledger coverage gap closed; UN-20 streaming served-by consistency fixed (the HITL did
its job). Both additive, scoped, codex-PASS.
