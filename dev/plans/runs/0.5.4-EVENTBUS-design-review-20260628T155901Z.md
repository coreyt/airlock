# 0.5.4 DESIGN gate #3 (decisive re-gate) — codex review (promoted verdict)

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`
- **Target:** `dev/notes/design-request-event-bus.md` **rev 3** + anchored modules
- **Branch / base:** `feat/0.5.4-eventbus` @ base `57010d1`
- **Review log:** `dev/plans/runs/0.5.4-EVENTBUS-design-review-20260628T155901Z.log`
- **Prior gates:** #1 BLOCK (4 findings), #2 BLOCK (3 findings) — all confirmed resolved here.

---

## Verdict: BLOCK  (no high finding — 1 medium + 1 low)

### 1. [medium] Metrics failure counter can be dropped
Refs: design-request-event-bus.md:231/253, metrics.py:202/216/240

§5a said metrics are "success only", but `log_failure_event()` increments
`requests_total{success="false"}` and metrics self-registers sync+async **failure**
callbacks too. The note only pinned "no failure `request_duration` row" — true but
incomplete. Behavior-preservation gap: MIGRATE-sidechannels could unknowingly drop
all failure request counters.

### 2. [low] Fast monitor inventory understates failure coverage
Refs: design-request-event-bus.md:230, monitor.py:383/389/390, config.yaml:548

Table said monitor self-registers sync+async **success**, but code registers
success **and** failure, and config includes both lists. Non-blocking (monitor stays
outside the sink seam) but the table should be corrected.

## What passed on inspection
Gate #2 findings 1 & 2 resolved: rev 3 carries raw `response_obj` with per-projection
serialization (matches fathom's `_response_text(response_obj)`); fathom whole-sink
skip correctly required as an emit predicate (matches enhanced_passthrough). Fathom
async-only, s3/sql opt-in/no self-register, enterprise config+self-registration all
correctly captured.

---

## Orchestrator triage

**Call: FIX (final) → terminal re-gate #4.** Gate #3 has **no high finding** — both
items are factual corrections to the §5a registration table, with
contract-determined resolutions verified against HEAD (`metrics.py:175-221`,
`monitor.py:389-392`). The design's *soundness* is validated (codex's "what passed"
now covers the event shape, raw-response_obj, fathom skip, async-only fathom, s3/sql
opt-in, enterprise registration, and monitor-ordering). The medium is a real
regression risk (dropped failure `requests_total`) and is now pinned as a
MIGRATE-sidechannels golden-test obligation.

Fixes applied in rev 4:
1. **F1 (medium)** — §5a table: metrics registers sync+async × success+failure; new
   **§5b** pins counter semantics — `requests_total` on BOTH paths (MUST NOT drop the
   failure counter), `request_duration`/`_record_mutations` success-only.
2. **F2 (low)** — §5a table: monitor registers sync+async × success+failure.

**Convergence:** gate findings 4 (2 high) → 3 (1 high) → 2 (0 high). The contract
forbids overriding a BLOCK, so I do not proceed on this verdict; I run one **terminal**
gate #4 over rev 4. **Gate #4 rule:** PASS → Phase E (`EVENT`); ANY BLOCK → halt to
HITL (no further autonomous re-gates).
