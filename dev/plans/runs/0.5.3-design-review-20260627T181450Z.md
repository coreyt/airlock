# 0.5.3 — DESIGN GATE review (Phase D)

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`
- **Scope reviewed:** `dev/plans/0.5.3-plan.md` + `dev/plans/runs/0.5.3-ACL-inventory.md`
  + the audit (Parts 1 & 3) + Presidio site (`pii_guard.py`) + `transparency.py`
  + `threat_detector.py` + the import-cycle anchors.
- **Review log:** `dev/plans/runs/0.5.3-design-review-20260627T181450Z.log`
- **Target:** `main` @ `fc67c33` (v0.5.2) — pre-Phase-E.

---

## Verdict: CONCERN

(No BLOCK. All findings are addressable by the orchestrator during pack authoring;
the plan's design — the ACL boundary, the Presidio offload, the parity surface — is
sound. Q1–Q4 boundary questions all answered.)

### 1. [medium] ACL scope is broader than the pack row
Refs: `dev/plans/0.5.3-plan.md:131`, `dev/plans/runs/0.5.3-ACL-inventory.md` §E

The inventory includes `health.py`, `proxy_errors.py`, `models_seam.py`, `docs.py`
proxy-app reads, but the ACL pack's one-line touch list omits them. If an
implementer follows the pack row instead of the inventory, the ACL misses real
internal reads.

### 2. [medium] Pack overlap is understated
Refs: `dev/plans/0.5.3-plan.md:125,131,132`

LATENCY and DECOUPLE both touch `fast/guardian.py` and `guardrails/extract.py`;
RACE also touches `fast/guardian.py`. They are NOT disjoint. Sequence must be
pinned, not run blindly in parallel.

### 3. [medium] Q2 must be pinned as mechanism ownership
Refs: `dev/plans/runs/0.5.3-ACL-inventory.md` Q2, `airlock/models_seam.py:220`

The app-object resolver alone is insufficient: the `middleware_stack is None` vs
"wrap a built stack" branch is the same version-coupled lifecycle mechanism
repeated across callers. The ACL should own a generic install helper;
`proxy_bootstrap.py` owns only subsystem order.

### 4. [low] Parity surface is right, but fixtures must be named
Refs: `dev/plans/runs/0.5.3-ACL-inventory.md` §parity, `dev/architecture.md:469`

"Every §3.7 ambiguity case" must become explicit fixtures: Anthropic/Bedrock/Vertex,
OpenAI/Azure, Vertex/Gemini split by `api_base`, streaming `vertex_ai_beta`, and
unknown-provider omission — not a prose cross-reference.

### Q1–Q4 (codex recommendations, ACCEPTED)

- **Q1:** ACL owns served-backend + cost extraction *from `_hidden_params`*, with a
  `cost_fallback=kwargs.get("response_cost")` parameter. Bare `kwargs.get("response_cost")`
  reads in the loggers (`fathom_logger`, `enterprise_logger`, `monitor` kwargs path)
  are the **documented CustomLogger surface** and stay outside the ACL.
- **Q2:** ACL owns `resolve_proxy_app()` **plus** the generic ASGI middleware-install
  mechanics (incl. the pre-/post-start `middleware_stack` branch). `proxy_bootstrap.py`
  owns install **order** only.
- **Q3:** `batch/middleware.py`'s `custom_llm_provider` **query param** is OUT of ACL
  scope (client/request surface, not a LiteLLM internal read). Confirmed.
- **Q4:** Confirmed. The LiteLLM 1.89.0 `_hidden_params` contract note lives in
  `litellm_adapter.py`. `_patch_lowest_cost_none_guard` (`enterprise_logger.py:549`)
  stays OUT of 0.5.3 ACL scope (defensive monkey-patch, not an internal read).

---

## Orchestrator triage (gate decision: PASS-after-fix → proceed to Phase E)

CONCERN with no BLOCK. I resolve all four findings by tightening the **pack prompts**
(no plan redesign required); the gate is cleared for Phase E. Resolutions:

- **F1 → FIX in ACL prompt.** The ACL pack's authoritative scope is
  `dev/plans/runs/0.5.3-ACL-inventory.md` (all of §A–E), NOT the plan's abbreviated
  row. The ACL MODIFY list will explicitly include `health.py`, `proxy_errors.py`,
  `models_seam.py`, `docs.py` for the proxy-app resolver migration.
- **F2 → FIX the parallelization plan.** Replace "LATENCY/RACE disjoint ∥" with
  conflict-free **waves** derived from the file-sharing graph:
  - `model_override_headers.py`: ACL + DECOUPLE
  - `fast/guardian.py`: DECOUPLE + LATENCY + RACE
  - `guardrails/extract.py`: DECOUPLE + LATENCY
  - **Wave 1: ACL ∥ RACE** — provably disjoint (ACL = transparency/monitor/
    model_override_headers/batch/admin/health/proxy_errors/models_seam/docs +
    new adapter; RACE = threat_detector/state/guardian/client_identity). guardian
    is touched by RACE only in Wave 1, so no contention.
  - **Wave 2: DECOUPLE** — cut after BOTH ACL (shares model_override_headers) and
    RACE (shares guardian) merge. Establishes the neutral text seam.
  - **Wave 3: LATENCY** — cut after DECOUPLE merges (shares guardian + extract);
    builds the per-request text cache on top of the new seam.
- **F3 → FIX in ACL + DECOUPLE prompts.** ACL exposes `resolve_proxy_app()` and a
  generic `install_asgi_middleware(app, factory)` encoding the None/built-stack
  branch once; `proxy_bootstrap.py` (DECOUPLE) calls them in the load-bearing order
  (batch → admin → header → LiteLLM).
- **F4 → FIX in ACL prompt.** Enumerate the named parity fixtures verbatim in the
  ACL pack's target-test list.

Phase E proceeds with the wave plan above. ACL and RACE prompts authored next.
