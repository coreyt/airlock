# Codex review — Pack 0.5.1-STORE-seam

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`
- **Target:** branch `feat/0.5.1-STORE-seam`, HEAD `4797d5f`
- **Baseline:** `1068125`
- **Review log:** `dev/plans/runs/0.5.1-STORE-seam-review-20260626T204219Z.log`
- **Date:** 2026-06-26

## Verdict: CONCERN

### 1. [medium] Cutoff bucket can include out-of-window spend
Refs: `airlock/fast/state.py:334,342,354,361`, `tests/test_fast_spend_store.py:102`

`recent_spend()`/`export_buckets()` keep every bucket whose index is
`>= floor((now - window) / bucket_width)`. With the default 1h bucket, spend just older
than 24h that falls in the cutoff bucket is still counted and checkpointed — overstating
by up to one bucket width. The test only uses `now - 90000` (clearly outside), so the
boundary is not exercised.

### 2. [low] Closure artifact head is stale
Refs: `dev/plans/runs/0.5.1-STORE-seam-output.json:5`

`head_sha` is recorded as `a30a89b` while the reviewed branch HEAD is `4797d5f` (the
closure-chore commit that added output.json itself). Bookkeeping only; source aligns.

## What passed on inspection
R5 cap removed (bucketed `SpendStore`, >1000-event tests present); integer µ$ storage /
float-USD return; explicit TTL > window; shared `RLock` around spend RMW; raised cache max
size. FIX-1 correctly child-located: `proxy.py` sets `AIRLOCK_LITELLM_CHILD=1`, launcher
SIGTERM no longer checkpoints, `monitor.py` gates restore/checkpoint/daemon-thread/atexit/
SIGTERM on that flag; spend uses a versioned sibling `spend_state.json`, temp + `os.replace`,
prune-before-checkpoint, restore via absolute `set_cache` (idempotent), breaker keeps its
5-min gate while spend restore is age-bounded only. Smoketest restart scenario added +
documented as operator-run.

## Review caveat (noted, not disqualifying)
codex's read-only sandbox intermittently failed command startup (`bwrap: loopback`), so it
could not run tests/`git`; review is from fresh file reads. The implementer's own run was
2125 passed / 73 skipped (one pre-existing unrelated `fathomdb` extra failure). Orchestrator
re-verifies the targeted spend tests post-fix and runs the live restart-durability smoke at
sign-off.

## Orchestrator triage — FIX both (no override)

- **Finding 1 (medium):** the ≤1-bucket trailing-edge overage is the *safe* direction for
  budget protection (warns/swaps slightly early, never late) — but it is currently
  undocumented and untested. FIX: document the conservative bucket-granularity semantic on
  `recent_spend()` (and the plan's window-semantics note), and add a boundary test pinning
  the behavior. Tighten the edge only if cheap; do NOT switch to an understating edge
  (understating spend would let real spend exceed budget — unsafe).
- **Finding 2 (low):** correct `output.json.head_sha` to the actual reviewed HEAD.

Resolution recorded in the STORE-seam fix commit + re-verification below.
