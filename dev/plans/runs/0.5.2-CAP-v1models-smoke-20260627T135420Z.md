# 0.5.2 CAP-v1models — live /v1/models smoke (isolated instance)

**When:** 2026-06-27 · isolated `:4137`, copied config (seam active), `/health/liveliness`
only. Live `:4000` untouched.

**Gate:** the additive `airlock` object surfaces on `GET /v1/models` and the standard
OpenAI fields stay intact (non-breaking) — UN-21 enumerate + UN-22 declared capabilities.

## Result — PASS

- Top-level `object: "list"` preserved; **73/73 models carry an additive `airlock`** object;
  **0 models missing** the standard `id`/`object` fields (every model also has `created`/`owned_by`).
- Values match `/model/info` exactly:
  - `aistudio/gemini-3.5-flash` → `{airlock_provider: gemini, endpoints: [chat,batch], underlying: gemini/gemini-3.5-flash, region: null, deprecated: false}`
  - `vertex/gemini-3.5-flash` → `{vertex_ai, [chat], vertex_ai/gemini-3.5-flash, region: global, false}` ⬅ region-gated (no false batch)
  - `gemini-3.5-flash-aistudio` (legacy twin) → `deprecated: true`
  - `anthropic/claude-haiku` → `{anthropic, [chat], …, false}`

A client ignoring `airlock` sees an unchanged OpenAI `/v1/models`. Combined with
`/model/info` (CAP-modelinfo) and `X-Airlock-Served-By` (NAME-aliases smoke), the
**discover → pin → verify** loop is proven end-to-end on the wire. Production safety: ✅.
