# 0.5.4-MIGRATE-entfathom-cutover (2b-ii) — codex pack review (promoted verdict)

- **Reviewer:** codex `gpt-5.5`, high effort, read-only
- **Target:** branch `worktree-agent-ac63240cce3fa8b85`, commits `d466934..70420d0`
- **Baseline:** `d466934`
- **Review log:** `dev/plans/runs/0.5.4-MIGRATE-entfathom-cutover-review-20260628T174626Z.log`

## Verdict: CONCERN

### 1. [med] No-double-emit proof misses callback-list cardinality
Refs: test_recorder_cutover.py:112/135, recorder.py:53

The no-double-emit test invokes a fresh `RequestRecorderCallback` directly (proves one
invocation emits once), and the async-list test only asserts the OLD
`proxy_logger`/`proxy_fathom_logger` are absent — neither proves litellm's live lists
contain `recorder_callback` exactly once (no duplicate) given `_self_register()` adds to
all 4 lists AND config.yaml also lists it. (Behavior relies on litellm dedup, exactly as
the old enterprise logger did — but it's unproven here.)

### 2. [low] codex could not run repo-wide grep / pytest (sandbox `RTM_NEWADDR` block)
Refs: output.json:18 — couldn't certify "no dangling deleted symbol" or rerun tests.

## What passed (codex)
Deletion clean (enterprise/fathom keep record_event + helpers + precall/batch/admin +
lowest-cost patch + dedup/engine); activation+ordering match spec (recorder first in all
4 lists + before monitor in config; seam test asserts enterprise record excludes
`airlock_provider_protection`); fathom gating correct (flag-gated, async-only, skip
honored; proxy.py no longer appends fathom); frozen oracle meaningful (timestamp-only
strip, full-dict + key-order compare, captured from live builders); **adapted tests
preserve their assertions** (re-pointed through event/projection/sink, not weakened) —
codex spot-checked all 5 touched test files.

---

## Orchestrator triage

**Finding 2 [low] → CLOSED by orchestrator verification.** Ran what codex's sandbox
couldn't (in the worktree): repo-wide grep shows the only "matches" are docstring
mentions + the unrelated `_build_recorder`/litellm lowest-cost patch — **no real
dangling references to any deleted symbol**; the broad no-network sweep (projections,
seam, wiring, request_event, enterprise, fathom, proxy, phase4, ratelimit) = **250
passed**. Implementer's full-suite claim (2515 passed) corroborated on this subset.

**Finding 1 [med] → FIX (add cardinality/idempotency test).** The behavior is almost
certainly correct (config + `_self_register` dedupe to one — the exact mechanism the
old enterprise logger relied on for its whole history), but no-double-emit is the
critical property and must be proven, not assumed. Resume the implementer to add a test
that exercises the LIVE registration: after `recorder._self_register()` (called twice
for idempotency), assert each of litellm's 4 callback lists contains `recorder_callback`
**exactly once** (dedup holds) and that the old `proxy_logger`/`proxy_fathom_logger` are
absent — i.e. no path double-emits. If the test reveals dedup does NOT hold for the
recorder, fix the registration (e.g. self_register async-only + config for sync). On
green → merge.
