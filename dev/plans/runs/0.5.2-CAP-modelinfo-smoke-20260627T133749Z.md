# 0.5.2 CAP-modelinfo — live /model/info smoke (isolated instance)

**When:** 2026-06-27 · **Who:** orchestrating agent · **How:** isolated `:4137`, copied
config (capability injection active), `/health/liveliness` only. Live `:4000` untouched.

**Gate:** prove the computed `model_info` actually surfaces on LiteLLM-native
`GET /model/info` for the pinned version (UN-22) — and that the region-gated batch rule
and the `enhanced/`→gemini / deprecated / served-by-token values are correct on the wire.

## Result — PASS (73/73 models carry computed model_info)

| model_name | airlock_provider | endpoints | underlying | region | deprecated |
|---|---|---|---|---|---|
| `anthropic/claude-haiku` | anthropic | [chat] | anthropic/claude-haiku-4-5-20251001 | — | false |
| `gemini-3.5-flash` (bare) | gemini | **[chat]** | gemini/gemini-3.5-flash | — | false |
| `aistudio/gemini-3.5-flash` | gemini | **[chat, batch]** | gemini/gemini-3.5-flash | — | false |
| `vertex/gemini-3.5-flash` | vertex_ai | **[chat]** ⬅ region-gated (global) | vertex_ai/gemini-3.5-flash | global | false |
| `gemini-3.5-flash-aistudio` | gemini | [chat, batch] | gemini/gemini-3.5-flash | — | **true** |
| `mistral/mistral-large` | mistral | [chat, batch] | mistral/mistral-large-latest | — | false |
| `vllm/qwen3.6-27b` | openai | [chat, batch] | openai/qwen3.6-27b | — | false |
| `aistudio/gemini-coding` | **gemini** (enhanced/→gemini) | [chat] | enhanced/gemini-coding | — | false |

**Proven live:** (1) the **region-gated** vertex batch rule — `vertex/…` at
`vertex_location: global` advertises `[chat]`, never a false `batch` (codex-BLOCK-3
fix on the wire); (2) the `enhanced/`→`gemini` classifier; (3) `deprecated:true` on the
legacy suffix twins; (4) `airlock_provider` = served-by token (vllm→openai); (5) bare =
sync-only vs consolidated `provider/` = chat+batch. **Production safety:** ✅ `:4000` untouched.
