# 0.5.4-MIGRATE-sql — code review (promoted verdict)

- **Reviewer:** **`code-reviewer` (sonnet) FALLBACK** — codex was rate-limited (usage
  limit, resets ~14:14); per `dev/agent-harness-reference.md` §3.2.
- **Target:** branch `worktree-agent-a0583b43fc258978a`, commits `e1c6382..ac2fc6e`
- **Baseline:** `e1c6382`
- **codex attempt log (aborted):** `dev/plans/runs/0.5.4-MIGRATE-sql-review-20260628T183236Z.log`

## Verdict: PASS

### 1. [low] async test now exercises sync dispatch path — correctly scoped, not a regression
The old `async def` test (which `await`ed `async_log_success_event`) is replaced by a sync
test routing through `recorder.dispatch(is_async=True)`; the old async method is deleted,
the sql sink is normal (not async_only), and the count assertion is preserved. Nit only.

### 2. [low] `skipif(not _SA_AVAILABLE)` on the disabled-path test — legitimate
`_ensure_initialized` raises ImportError (no sqlalchemy) before the URL check, so the
"URL unset → no engine → no raise" semantics can't be reached without SA; pre-existing
behavior, recorder dispatch catches the ImportError at the sink boundary anyway. Not masking.

## What passed (reviewer)
- **project_sql fidelity (JSON-string):** verbatim structural match to the deleted
  `_build_record` — same key order, identical `json.dumps(event.messages, default=_serialize)`
  + double-encoded response, `event.bare_exception_error == str(kwargs.get("exception"))|None`,
  same `_serialize` reused → byte-identical JSON. No redaction, no superset fields.
- **Deletion clean:** `_build_record` + 4 callback methods gone; dropped imports genuinely
  unused; `_insert`/`_ensure_initialized`/engine/table/disabled-path/instance kept byte-for-byte.
- **Gating:** sql sink behind `AIRLOCK_ENABLE_SQL_LOGGER`, normal sink; enterprise/fathom/s3
  unaffected; both flag-absent/present tested.
- **Frozen oracle:** all 8 scenarios; `error:"None"` string edge + `response is None` covered;
  key-order asserted.
- **No weakened assertions** in test_sql_logger.py (all 6 DB round-trip asserts + failure
  `"timeout" in error` + JSON decode + multi-insert count preserved).

## Orchestrator triage
**PASS → merge.** Both nits are low and confirmed non-regressions. Orchestrator already
verified 78 keystone tests + 2553 full-suite green in the worktree. Merge sql; next
MIGRATE-sidechannels (prefer codex review once it resets ~14:14).
