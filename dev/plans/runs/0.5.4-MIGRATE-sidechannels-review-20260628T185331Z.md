# 0.5.4-MIGRATE-sidechannels — code review (promoted verdict)

- **Reviewer:** **`code-reviewer` (sonnet) FALLBACK** — codex still rate-limited (usage
  limit; probe at 18:53Z still erroring); per `dev/agent-harness-reference.md` §3.2.
- **Target:** branch `worktree-agent-a548fe6ba11eed565`, commits `971c8ae..ea8433e`
- **Baseline:** `971c8ae`

## Verdict: PASS

### 1. [low] `_record_mutations_from_event` not crash-safe for non-dict mutation items → FIXING
Refs: metrics.py:158, request_event.py:189-192, enterprise_logger.py:44-54

`event.mutations` is `asdict(m)` for dataclasses else `_serialize(m)` (→ `str` fallback). If
a non-dataclass item ever reached `airlock_mutations`, `m.get("field")` would raise
`AttributeError` (crashing `record_event` for that request), where the old
`getattr(m,"field",None)` silently skipped it. Unreachable in practice (transparency.py
always emits `Mutation` dataclasses) and caught by `RequestRecorder.dispatch` isolation —
but a genuine crash-safety divergence from the old code. Behavior-preservation is the
release contract → hardening to restore exact parity (skip non-dict items gracefully).

### 2. [low] Two mutation test cases collapse to the same scenario — cosmetic
absent vs empty `airlock_mutations` both → `event.mutations == []` post-migration (behavior
genuinely identical); add a clarifying comment. Nit.

### 3. [low] cardinality test depends on suite import order — stable (self_register removed). No action.

## What passed (reviewer)
Counter fidelity bit-identical (both paths; `user or "unknown"`; `request_duration` from raw
datetimes success-only; mutations success-only). Mutation dict-access equivalent to the old
`getattr` for the real `Mutation` path (same field/op, same skip rule, TypeError guard kept).
Clean deletion (callback methods + `_self_register` def+call gone; 6 guardrail helpers
untouched). Always-on unconditional registration. No double-emit (`metrics_callback` absent
from litellm lists; one dispatch = one increment). **No weakened tests** — old "in lists"
assertions correctly replaced; cutover test net-strengthened.

## Orchestrator triage
**PASS → FIX nit 1 (cheap crash-safety parity) → merge.** Resume the implementer to harden
`_record_mutations_from_event` (handle/skip non-dict items, matching the old getattr safety)
+ add the nit-2 comment, re-test, then merge. (Orchestrator already verified 39 keystone
tests + 2561 full-suite green.)
