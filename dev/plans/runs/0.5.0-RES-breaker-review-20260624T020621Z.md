# Pack 0.5.0-RES-breaker review (re-run, file-based) — codex gpt-5.5 (20260624T020621Z)

---

## Verdict: BLOCK

Findings:

- BLOCK `airlock/fast/state.py:386`: provider-level half-open failure is not implemented. `ProviderState.record_rate_limit()` records the 429 but never checks or clears `_half_open_probe`, and `StateStore.record_provider_rate_limit()` only provider-quarantines through impacted-client escalation at `airlock/fast/state.py:791`. A provider probe-mode clear followed by one 429 will not immediately re-arm the provider, and `_half_open_probe` can stay set. Violates CC-7.

- BLOCK `airlock/fast/state.py:429`: `ProviderState.impacted_clients()` only applies `ProviderState.cleared_at`. The CC-6 design says impacted-client escalation must also respect each client-provider bucket’s `cleared_at`; otherwise a cleared client bucket can still contribute pre-clear provider events to provider escalation. Current `rate_limit_events` only stores `(timestamp, client_id)`, and `impacted_clients()` has no access to per-client floors.

- BLOCK `airlock/fast/state.py:759`: disabled clients can still contribute to later provider escalation. The disabled path records the provider event before returning, but the impacted filter at `airlock/fast/state.py:791` excludes only `escalation_exempt`, not `disabled`. A disabled client’s 429 plus a normal client’s later 429 can satisfy the default provider threshold of 2. Violates “disabled never arms/escalates but still records.”

- CONCERN `airlock/fast/state.py:102`: env default precedence is incomplete for config-defined clients. Config clients are resolved before env defaults are applied, so an env default does not flow into existing config client policies unless the same client is repeated in env. That does not match “env > config” plus “per-client > default > constant.”

- CONCERN `airlock/fast/state.py:115`: malformed env shape can still crash. `AIRLOCK_BREAKER_OVERRIDES='{"clients":[]}'` reaches `.items()` on a list, raising `AttributeError`, which is not caught at `airlock/fast/state.py:121`. The malformed-env requirement says defaults + warning, no crash.

- CONCERN `airlock/fast/monitor.py:210`: below-threshold 429s are still logged as `action="client_quarantine"` whenever provider quarantine did not happen, even when `outcome["client_quarantined"]` is false. That is a caller regression risk with threshold > 1, and `ingest_jsonl_record()` keys on that action at `airlock/fast/state.py:943`.

Verified clean portions: default policy is one-strike/300s/escalation-2, client threshold counting records before checking so the Nth strike arms, client `cleared_at` floors `recent_rate_limit_count`, provider `cleared_at` floors provider-wide impacted history, escalation uses the triggering client’s threshold/cooldown policy, proxy wires `configure_breaker(config)`, and no admin clear mutators are added here. No tests or git commands run.
