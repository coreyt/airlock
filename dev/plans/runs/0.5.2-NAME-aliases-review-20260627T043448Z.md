# 0.5.2 NAME-aliases — Code Review (codex gpt-5.5, reasoning=high)

**Diff:** `feat/0.5.2-NAME-aliases` @ 3f4fc08 vs base 2f9b791 (inlined; unmerged local branch).
**Date:** 20260627T043448Z
**Outcome (round 1):** **BLOCK** — one real correctness bug; fixer in flight (same worktree).

## Orchestrator triage

**Round 1 verdict: BLOCK.** Valid catch — NOT merged.

1. **Ambiguous variant leaks to fuzzy (real, blocking).** `model_alias.load_from_config`
   correctly drops ambiguous variant keys from `_exact` (pass 2), but `resolve()` then
   falls through to `_score_match()` for a NON-slash ambiguous body (e.g.
   `gemini-3.1-pro-preview`, claimed by both AI-Studio and Vertex, no explicit alias) —
   scores an exact body match ~1.0 and silently picks the first entry. This is the silent
   cross-provider repoint the design (§4.1) said to prevent ("return None, no fuzzy, no
   cache"). The slash path was guarded; the non-slash bare body was not.
   → **Fix (in flight):** retain an `_ambiguous_variants` set from pass 2; in `resolve()`
   return `None` (no cache) before prefix-strip/fuzzy when `lower` (or the slash `bare`)
   is ambiguous and no explicit key matched. + a RED-first regression test for the
   dropped-ambiguous-variant path (codex finding #2).

**Action:** fixer resumed in the worktree (SendMessage to the implementer); re-review the
fix diff before merge. Everything else in the diff reviewed clean (two-pass loader, shared
`airlock_provider_for` classifier + router wiring, config dual-listing + consolidated batch
markers, no model_info, no scope creep).

## codex round-1 verdict (verbatim)

```
## Verdict: BLOCK

1. [airlock/fast/model_alias.py:386](<repo-root>/airlock/fast/model_alias.py:386) Ambiguous variants are only dropped from `_exact`, then can be resurrected by fuzzy scoring. Example: `gemini-3.1-pro-preview` is claimed by both AI Studio and Vertex entries, so pass 2 drops it, but `resolve("gemini-3.1-pro-preview")` reaches `_score_match()`, scores exact provider-body matches at `1.0`, and silently picks the first entry. Fix: retain an `_ambiguous_variants` set from pass 2 and return `None` before prefix-strip/fuzzy when `lower` or slash `bare` is in that set, unless an explicit key already matched.

2. [tests/test_model_alias.py:491](<repo-root>/tests/test_model_alias.py:491) The new tests cover the explicit `gemini-3.5-flash` case but miss the dropped-ambiguous-variant path above. Add a config with two providers sharing a provider body and no explicit bare alias, then assert the bare ambiguous body returns `None` and is not cached.
tokens used
## Verdict: BLOCK

1. [airlock/fast/model_alias.py:386](<repo-root>/airlock/fast/model_alias.py:386) Ambiguous variants are only dropped from `_exact`, then can be resurrected by fuzzy scoring. Example: `gemini-3.1-pro-preview` is claimed by both AI Studio and Vertex entries, so pass 2 drops it, but `resolve("gemini-3.1-pro-preview")` reaches `_score_match()`, scores exact provider-body matches at `1.0`, and silently picks the first entry. Fix: retain an `_ambiguous_variants` set from pass 2 and return `None` before prefix-strip/fuzzy when `lower` or slash `bare` is in that set, unless an explicit key already matched.
```
