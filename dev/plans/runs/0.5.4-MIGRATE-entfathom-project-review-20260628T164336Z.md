# 0.5.4-MIGRATE-entfathom-project — codex pack review (promoted verdict)

- **Reviewer:** codex `gpt-5.5`, `model_reasoning_effort=high`, `--sandbox read-only`
- **Target:** branch `worktree-agent-a32eb91b756a6372f`, commits `2935062..a0fc3cb`
- **Baseline:** `2935062` (feat/0.5.4-eventbus tip)
- **Review log:** `dev/plans/runs/0.5.4-MIGRATE-entfathom-project-review-20260628T164336Z.log`

---

## Verdict: BLOCK

### 1. [blocker] Fathom `cost` diverges for explicit `None`
Refs: fathom_logger.py:132, request_event.py:209, projections.py:67, test_projections_equiv.py:76

Old fathom uses `kwargs.get("response_cost", 0)`: `{"response_cost": None}` → `cost=None`
→ dropped by the final None-filter. The projection maps `event.response_cost is None`
→ `0`, emitting `cost: 0`. Real field-presence/value divergence; fixtures always set a
numeric cost so it passed.

### 2. [concern] Golden tests don't verify key order or guardrail-collision cases
Refs: projections.py:42, test_projections_equiv.py:218/231

Spread order matches the oracle (`**usage`,`**mcp_meta`,`**guardrail_meta`, then
mutations/served/attribution/airlock_client), but tests use dict-equality (order-blind)
and no fixture puts a colliding `airlock_provider`/`airlock_client` in `guardrail_meta`
— the case where spread/update order is observable.

## What passed
`project_enterprise` faithful (keys/values/spread order/post-literal assignments);
`project_fathom` faithful for base props, env-gated additions, MCP fields, None-filter —
except the explicit-None cost. Scope additive (projections.py + test + memo only).

---

## Orchestrator triage

**Call: FIX (blocker + concern) → re-verify → merge.** Both are real keystone-equivalence
issues (2b deletes the old builders and relies on these projections), both cheaply
fixable in the 2a worktree.

- **F1 (blocker):** `response_cost` is a **fathom-only** field on the event (enterprise
  record omits it; served uses a separate `kwargs.get("response_cost")` call inside the
  builder). So source it faithfully at build: `request_event.py` →
  `response_cost = kwargs.get("response_cost", 0)` (apply fathom's `.get(...,0)` default
  AT SOURCE), and `projections.py` → `"cost": event.response_cost` (raw; the None-filter
  drops explicit-None). This reproduces all three cases: absent→0(kept), None→omitted,
  value→value. Authorized to touch the merged `request_event.py` (1-line correctness fix
  to the fathom-only field) + update `tests/test_request_event.py` if it asserts
  `response_cost` on a fixture that omits the key.
- **F2 (concern):** add a fixture whose `guardrail_meta` contains `airlock_provider` and
  `airlock_client` (collision case) and assert enterprise equivalence holds; add a
  **key-order** assertion (`list(got) == list(expected)`) to the enterprise golden test
  to lock the spread order; add fathom fixtures for absent / explicit-None / numeric cost.

Resumed the 2a implementer to apply, re-test, commit, update output.json. On green →
merge 2a → author 2b.
