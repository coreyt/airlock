# Pack 0.4.0-A fix-1 Re-Review — codex gpt-5.5 verdict

Reviewer: `codex exec --model gpt-5.5 -c model_reasoning_effort=high --sandbox read-only`.
Target: branch `0.4.0-A-fix1` HEAD `81c567e` (commits `930419b..HEAD`: RED
`0e83e08` → GREEN `81c567e`).
Baseline: `930419b` (Pack A GREEN).
Prior verdict: BLOCK (`0.4.0-A-review-20260615T032242Z.md`).
Review log: `dev/plans/runs/0.4.0-A-fix1-review-20260615T115144Z.log`.

## Verdict: PASS

No findings.

## Addressed
The prior **high BLOCK** is resolved: caller-controlled `input_file_id` /
`purpose == "batch"` markers no longer let a non-batch completion route classify
itself as batch and skip model guardrails. `is_batch_call()` now treats a
non-empty `call_type` as authoritative (`call_type in _BATCH_CALL_TYPES` only);
data markers are a fallback consulted only when `call_type` is empty, and a
completion-shaped payload (`messages`/`prompt`/`input`) wins.

## What passed on inspection
- Bypass closed (`extract.py:116-135`); guardian gating unchanged but now reached
  by normal completions (`guardian.py:169-201,284,310`).
- Empty-`call_type` fallback safe; real batch on unknown call_type still detected.
- No new false-negative for the listed LiteLLM batch/file routes. Residual: future
  batch call-type names must be added to `_BATCH_CALL_TYPES` (not newly weakened).
- Tests pin the security property directly, not just the happy path
  (`tests/test_extract.py:240-256`).

## Orchestrator triage
**PASS — accepted.** Merged to main as `e35ab66` (`git merge --no-ff`). 77 tests
green on main post-merge. Both worktrees removed, branches deleted. Pack A CLOSED.

Sandbox note: codex could not run `git`/`pytest` (read-only bwrap blocked
loopback); it inspected files directly at the branch. Orchestrator independently
re-ran the 77 tests on main.
