services:
  airlock:
    build: .
    ports:
      - "${AIRLOCK_PORT:-4000}:4000"
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      # Liveness only — answers "is the process responsive", never calls a model.
      test: ["CMD", "curl", "-f", "http://localhost:4000/livez"]
      interval: 30s
      timeout: 5s
      retries: 3
